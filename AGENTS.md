# 项目协作约束

本文件适用于整个项目目录及其所有子目录。

## 开发进度清单（阶段任务强制入口）

1. 根目录 `DEVELOPMENT_PROGRESS.md` 是开发任务的首要交接入口，但不替代对应的修改计划文档。
2. 每次开始或恢复阶段 0–8 的开发、阶段内修复或阶段验收前，必须先完整读取 `DEVELOPMENT_PROGRESS.md`，再读取对应的 `docs/YYYY-MM-DD/*计划.md`。
3. 读取清单后、修改任何代码、配置或文档前，必须核对并记录：
   - `git rev-parse --show-toplevel` 返回的工作区绝对路径；
   - 当前分支和短 HEAD；
   - `git worktree list --porcelain` 中的并行工作区；
   - `git status --short` 中的已有修改及其归属；
   - 当前阶段状态、最近验证、阻塞项和一条可直接执行的下一步。
4. 对应阶段计划必须包含“开工记录”，明确写出已读取进度清单的日期、工作区、分支和基线 HEAD；缺少该记录不得开始实施。
5. 开始任务时在清单“活动任务”中登记为 `进行中`；达成可验证检查点、改变范围、切换分支或 worktree、暂停、阻塞、完成时同步更新。
6. 暂停、交接或完成前必须记录最近验证命令与结果、未提交变更归属、阻塞项和下一项具体动作。不得只写“继续开发”“待处理”等无法直接接手的描述。
7. 多 worktree 并行时每个任务保留独立记录，不得覆盖或推断其他工作区状态；未经核实的内容使用 `需核实`。
8. 清单与 Git 或测试结果不一致时，以实际状态为准，先修正清单和阶段计划，再继续开发。

## 修改计划文档

1. 任何代码、配置或文档修改开始前，必须先创建对应的修改计划文档。
2. 计划文档统一保存到 `docs/YYYY-MM-DD/文件名称.md`。
3. 日期使用执行修改当天的本地日期，格式必须为四位年份、两位月份和两位日期。
4. 文件名必须清晰描述本次修改内容，并以 `计划.md` 结尾，例如 `项目基础架构搭建计划.md`。
5. 每个独立修改任务使用一份计划；同一任务的需求调整继续更新原计划，不重复创建同义文件。
6. 计划至少包含以下内容：
   - 修改目标；
   - 修改范围与明确不包含的内容；
   - 涉及的文件；
   - 实施步骤；
   - 验证方式与通过标准。
7. 实施过程中如范围、文件或步骤发生变化，必须先更新计划，再继续修改。
8. 修改完成后，在计划中补充实际结果和验证结果，确保计划与最终交付一致。
9. 除非用户明确要求其他位置，不得将新的修改计划写入 `docs/`、项目根目录或其他目录。
10. 阶段计划除既有必填内容外，还必须包含本文件“开发进度清单”要求的开工记录。

## 范围控制

1. 严格按照当前计划实施，不顺带加入未列入计划的功能、依赖或重构。
2. 如发现需要扩大范围，先更新计划；如果会显著改变交付结果，先取得用户确认。
3. 所有完成声明必须以实际执行的检查、测试或运行结果为依据。

## README 目录契约

1. 每个受版本控制且由项目维护的目录必须包含 `README.md`。
2. 修改目录内任意文件时，同目录 `README.md` 必须在同一变更中同步更新。
3. 每份目录 README 至少包含目录用途、内容说明、依赖边界、维护规则和变更同步。
4. `README` 检查排除 `.git`、`.worktrees`、`.superpowers`、`node_modules`、`out`、`dist`、`coverage`、缓存和临时目录。
5. 提交前必须运行 `npm run check:readmes`；新增目录时先添加 README，再添加目录内其他文件。

