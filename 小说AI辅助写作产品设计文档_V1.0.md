# 小说 AI 辅助写作产品设计文档

**文档版本：** V1.0  
**产品形态：** Windows 桌面应用  
**技术方向：** Electron + Vue 3 + TypeScript、本地优先、用户自备模型 API Key  
**文档用途：** 产品评审、原型设计、研发拆解、AI 能力规划、MVP 验收

---

## 1. 文档背景

### 1.1 产品背景

长篇小说创作通常包含创意构思、世界观设定、人物设计、故事大纲、章节规划、正文写作、内容润色、设定校验和版本修改等多个环节。

通用 AI 对话工具虽然能够辅助写作，但存在以下问题：

1. 小说内容分散在多轮对话中，缺乏统一项目管理。
2. 作品篇幅增加后，AI 容易遗忘人物、时间线和前文设定。
3. 用户无法清晰管理世界观、大纲、人物、章节和正文之间的关系。
4. AI 生成过程缺少可重复、可配置的工作流。
5. 同一内容多次生成后，缺少版本对比和回滚能力。
6. 不同模型、提示词和写作方法难以统一管理。
7. 用户需要手动整理上下文，创作效率仍然较低。

本产品通过“结构化小说资料 + AI 对话 + 提示词模板 + 上下文管理 + 创作工作流”的方式，将通用 AI 能力转化为面向小说创作的专用工具。

---

## 2. 产品概述

### 2.1 产品名称

暂定名称：**NovelAI Studio / AI 小说创作助手**

正式名称可在品牌设计阶段确定。

### 2.2 产品定位

一款面向个人小说作者和网文创作者的本地优先 AI 辅助写作工具。

产品不代替作者完成全部创作，而是帮助作者：

- 整理创意；
- 设计世界观；
- 生成人物和关系；
- 生成并维护故事大纲；
- 规划章节；
- 基于已有资料生成正文；
- 检查前后设定冲突；
- 管理创作过程和版本；
- 降低长篇作品的上下文管理成本。

### 2.3 产品愿景

让作者能够将更多精力放在创意、人物和故事选择上，而不是反复整理资料、复制上下文和修复 AI 遗忘问题。

### 2.4 核心价值

| 价值 | 说明 |
|---|---|
| 创作结构化 | 将聊天中的创意转化为可编辑的世界观、人物、大纲和章节数据 |
| 长篇一致性 | 通过摘要、人物状态、时间线和伏笔管理降低设定冲突 |
| AI 可控 | 用户能够控制模型、提示词、技能、文风、字数和生成范围 |
| 内容可回退 | 每次生成和修改保留版本，避免内容被不可逆覆盖 |
| 数据自主 | 小说资料优先保存在本地，API Key 由用户自行配置 |
| 模型开放 | 通过统一适配层支持多种兼容接口和模型服务商 |

---

## 3. 产品原则

### 3.1 作者拥有最终决定权

AI 生成内容默认属于候选结果，重要设定需要用户确认后才写入正式资料。

### 3.2 对话是入口，结构化数据才是项目资产

用户可以通过对话提出要求，但系统需要将确认后的信息同步到人物、世界观、大纲、章节等正式模块。

### 3.3 不依赖模型永久记忆

软件负责保存小说内容，每次生成时由系统重新选择并组装相关上下文。

### 3.4 优先使用固定工作流

第一阶段使用明确、可验证的生成流程，不将核心功能建立在完全自主的 Agent 行为上。

### 3.5 重要内容不可静默修改

人物死亡、身份揭示、世界规则、关系变化、故事主线等内容，不允许 AI 在后台自动覆盖。

### 3.6 本地优先

项目文件、正文、提示词和 API 配置优先保存在本地。除用户选择调用模型外，不应主动上传小说内容。

---

## 4. 目标用户

### 4.1 核心用户

#### A. 网络小说作者

特征：

- 需要持续更新；
- 单章字数相对固定；
- 重视节奏、爽点和章节钩子；
- 需要维护大量人物和剧情线；
- 对生成速度和续写效率要求较高。

核心需求：

- 快速生成章节草稿；
- 保持人物和剧情连续；
- 管理伏笔、冲突和章节节奏；
- 对正文进行扩写、缩写和重写。

#### B. 小说创作爱好者

特征：

- 有创意但缺少完整写作方法；
- 不熟悉大纲和人物设计；
- 容易在创作中途放弃；
- 希望 AI 提供引导和建议。

核心需求：

- 通过对话逐步明确创意；
- 自动生成大纲和人物设定；
- 获得下一步创作建议；
- 简化复杂的写作流程。

#### C. 有经验的长篇作者

特征：

- 有自己的方法论和写作模板；
- 对 AI 输出可控性要求高；
- 重视版本、资料库和一致性；
- 希望自定义提示词和工作流。

核心需求：

- 自定义 Skill；
- 自定义模型和参数；
- 精细控制上下文；
- 对比多个生成版本；
- 检查人物、时间线和设定冲突。

---

## 5. 核心使用场景

### 5.1 从一个创意开始创建小说

用户输入：

> 我想写一部发生在海上封闭城市的悬疑小说，主角是一名失去部分记忆的调查员。

系统通过多轮对话补充：

- 类型；
- 目标篇幅；
- 叙事视角；
- 主角目标；
- 核心冲突；
- 世界规则；
- 结局方向；
- 写作风格。

对话完成后，系统生成项目基础资料。

### 5.2 生成完整故事大纲

系统基于小说创意和世界观生成：

- 故事简介；
- 核心冲突；
- 故事阶段；
- 分卷大纲；
- 章节规划；
- 主要转折；
- 高潮和结局；
- 主要伏笔。

用户可以接受、修改或重新生成局部内容。

### 5.3 生成人物设定

系统生成：

- 基础身份；
- 外貌特征；
- 性格；
- 欲望和恐惧；
- 背景经历；
- 能力和弱点；
- 人物弧光；
- 人物关系；
- 语言风格；
- 剧情作用。

用户确认后写入人物库。

### 5.4 根据章节规划生成正文

系统自动组装：

- 当前章节目标；
- 本章登场人物；
- 最近章节摘要；
- 上一章正文；
- 相关历史事件；
- 未回收伏笔；
- 当前人物状态；
- 写作 Skill；
- 用户本次要求。

然后生成正文草稿。

### 5.5 对正文进行局部修改

用户可以选中一段文字并执行：

- 润色；
- 扩写；
- 缩写；
- 改变语气；
- 增加对话；
- 增加动作描写；
- 加强冲突；
- 改变叙事视角；
- 保留剧情但重写表达；
- 根据批注修改。

### 5.6 维护长篇小说一致性

每章确认后，系统自动或经用户触发提取：

- 本章摘要；
- 新增事实；
- 人物状态变化；
- 人物关系变化；
- 时间线事件；
- 新增伏笔；
- 已回收伏笔；
- 新地点和新物品；
- 需要后续处理的冲突。

---

## 6. 产品范围

### 6.1 MVP 范围

第一版必须完成以下闭环：

1. 创建和管理小说项目；
2. 配置模型 API；
3. AI 对话；
4. 创建世界观基础资料；
5. 生成人物设定；
6. 生成故事大纲；
7. 生成章节规划；
8. 根据大纲和上下文生成正文；
9. 支持流式输出和停止生成；
10. 保存每次生成结果；
11. 支持重新生成和版本回退；
12. 自定义基础 Skill；
13. 项目本地保存；
14. TXT 导入和 TXT/Markdown 导出；
15. 自动生成章节摘要；
16. 基础一致性检查。

### 6.2 第二阶段范围

- 人物关系图；
- 时间线视图；
- 伏笔管理；
- 多模型对比；
- 章节批量规划；
- 上下文预算面板；
- Prompt 模板管理；
- 更细粒度的版本对比；
- 标签和全文检索；
- 根据章节自动更新人物状态；
- RAG 语义检索；
- EPUB、DOCX 等格式导出。

### 6.3 暂不纳入范围

- 自行训练或微调大模型；
- 云端多人实时协作；
- 小说发布平台；
- 读者社区；
- 自动商业化分发；
- 完全自主写完整本小说的 Agent；
- 复杂的本地模型部署管理；
- 移动端同步；
- 版权检测和法律判定。

---

## 7. 产品总体架构

### 7.1 桌面端技术实现

- Electron 负责 Windows 桌面容器、窗口生命周期和后续本地系统能力；
- Vue 3 + TypeScript 负责渲染进程界面；
- electron-vite 统一主进程、预加载层和渲染进程构建；
- 渲染进程启用上下文隔离与沙箱，关闭 Node.js 集成；
- 本地能力必须通过预加载层提供经过类型约束的最小接口。

### 7.2 分层架构

```text
用户界面层
├── 项目中心
├── AI 对话
├── 世界观
├── 人物库
├── 大纲
├── 章节树
├── 正文编辑器
├── Skill 管理
├── 版本历史
└── 设置中心

应用服务层
├── 项目服务
├── 文档服务
├── AI 任务服务
├── 上下文组装服务
├── Prompt 服务
├── 版本服务
├── 导入导出服务
└── 一致性检查服务

AI 能力层
├── 模型适配器
├── 提示词模板
├── 结构化输出解析
├── 流式输出
├── 重试与降级
├── Token 估算
└── AI 任务日志

数据层
├── 项目数据
├── 世界观数据
├── 人物数据
├── 大纲和章节
├── 正文和版本
├── 摘要和记忆
├── Skill
├── Prompt
└── 应用配置
```

---

## 8. 信息架构

### 8.1 项目中心

展示：

- 项目名称；
- 封面；
- 小说类型；
- 最近编辑时间；
- 当前字数；
- 当前章节；
- 创作进度；
- 使用模型；
- 项目状态。

操作：

- 新建项目；
- 打开项目；
- 复制项目；
- 导入项目；
- 导出项目；
- 归档项目；
- 删除项目。

### 8.2 创作工作台

建议采用三栏布局：

#### 左侧：项目导航

- AI 对话；
- 项目概览；
- 世界观；
- 人物；
- 关系；
- 大纲；
- 章节；
- 时间线；
- 伏笔；
- Skill；
- 版本历史。

#### 中间：主编辑区域

根据模块显示：

- 表单；
- 卡片；
- 树形大纲；
- 正文编辑器；
- 对话内容；
- 版本差异。

#### 右侧：AI 助手和上下文

显示：

- 当前任务；
- 已选择上下文；
- Token 预算；
- 已启用 Skill；
- 模型参数；
- AI 建议；
- 生成、停止、重试操作。

---

## 9. 核心业务流程

### 9.1 新建项目流程

```text
新建项目
→ 输入基础创意
→ 选择创建方式
   ├── 空白项目
   ├── AI 对话引导
   └── 导入已有文本
→ 补充项目偏好
→ AI 生成项目提案
→ 用户确认或修改
→ 创建正式项目
```

项目偏好包括：

- 小说类型；
- 目标字数；
- 目标章节数；
- 读者定位；
- 叙事视角；
- 文风；
- 节奏；
- 内容限制；
- 结局方向。

### 9.2 大纲生成流程

```text
读取项目设定
→ 读取用户要求
→ 生成大纲候选
→ 结构化解析
→ 用户预览
→ 接受 / 修改 / 重新生成
→ 保存大纲版本
→ 可选生成章节规划
```

### 9.3 人物生成流程

```text
读取故事主题和大纲
→ 选择人物类型
→ 生成人物候选
→ 用户确认
→ 保存人物
→ 建立人物关系
→ 关联章节和事件
```

### 9.4 正文生成流程

```text
选择章节
→ 检查章节规划
→ 组装上下文
→ 估算 Token
→ 用户确认本章要求
→ 调用模型流式生成
→ 生成正文候选
→ 用户接受 / 编辑 / 重写
→ 保存正文版本
→ 提取摘要和状态变化
→ 更新小说记忆
```

### 9.5 章节确认后的处理流程

```text
正文确认
→ 生成章节摘要
→ 提取事件
→ 提取人物状态
→ 提取关系变化
→ 提取伏笔变化
→ 执行一致性检查
→ 展示待确认变更
→ 用户确认后写入正式资料
```

---

## 10. 功能设计

## 10.1 项目管理

### 功能要求

- 支持新建、复制、重命名、删除和归档项目；
- 项目数据独立保存；
- 项目支持自动保存；
- 支持手动备份；
- 支持导入导出完整项目包；
- 展示项目统计信息；
- 支持项目恢复点。

### 项目基础字段

| 字段 | 类型 | 说明 |
|---|---|---|
| 项目名称 | 文本 | 小说名称或临时名称 |
| 简介 | 长文本 | 项目简介 |
| 类型 | 枚举/标签 | 玄幻、悬疑、科幻等 |
| 目标字数 | 数字 | 全书目标字数 |
| 目标章节 | 数字 | 预计章节数 |
| 读者定位 | 文本/标签 | 目标受众 |
| 叙事视角 | 枚举 | 第一人称、第三人称等 |
| 默认文风 | 文本/Skill | 全局写作要求 |
| 项目状态 | 枚举 | 构思中、创作中、已完成、归档 |
| 默认模型 | 配置引用 | 项目默认 AI 模型 |

---

## 10.2 AI 对话

### 产品定位

AI 对话不是普通聊天窗口，而是项目操作入口。

### 核心能力

- 对话讨论创意；
- 根据对话生成结构化内容；
- 将对话结果保存到指定模块；
- 从人物、大纲或章节页面发起上下文对话；
- 支持引用项目资料；
- 支持将回答转换为人物、设定、章节或 Skill；
- 支持重新生成；
- 支持对话分支；
- 支持清空当前对话但不删除项目数据。

### 推荐操作

AI 回答下方提供：

- 保存为世界观；
- 保存为人物；
- 保存为大纲；
- 保存为章节规划；
- 保存为 Skill；
- 插入正文；
- 作为新版本；
- 继续讨论。

---

## 10.3 世界观管理

### 数据分类

- 世界背景；
- 地理和地点；
- 历史；
- 社会制度；
- 势力和组织；
- 能力体系；
- 科技或魔法规则；
- 文化和宗教；
- 物种；
- 重要物品；
- 禁止修改的核心规则。

### 功能要求

- 支持分类和标签；
- 支持 AI 生成和扩写；
- 支持手动编辑；
- 支持锁定重要设定；
- 支持建立条目关联；
- 支持查看引用该设定的章节；
- 支持冲突检测；
- 支持设定版本。

---

## 10.4 人物管理

### 人物字段

| 分类 | 字段 |
|---|---|
| 基础信息 | 姓名、别名、年龄、性别、身份、职业、阵营 |
| 外在特征 | 外貌、衣着、动作习惯、说话特点 |
| 内在特征 | 性格、价值观、欲望、恐惧、秘密 |
| 能力 | 技能、优势、弱点、限制 |
| 背景 | 出生、经历、家庭、重要创伤 |
| 故事作用 | 主角、反派、配角、导师等 |
| 人物弧光 | 初始状态、转变节点、最终状态 |
| 当前状态 | 所在地、身体、情绪、持有物品、已知信息 |
| 关系 | 与其他人物的关系和变化 |
| 创作约束 | 不可改变内容、人物禁忌、语言风格 |

### 功能要求

- 支持人物卡片和详情页；
- 支持 AI 生成人物；
- 支持人物关系；
- 支持当前状态；
- 支持人物出场章节；
- 支持人物专属 Skill；
- 支持人物一致性检查；
- 支持人物状态版本。

---

## 10.5 大纲管理

### 大纲层级

```text
故事总纲
└── 分卷大纲
    └── 故事阶段
        └── 章节规划
```

### 故事总纲字段

- 作品主题；
- 核心卖点；
- 主角目标；
- 核心冲突；
- 主要阻碍；
- 故事起点；
- 中段转折；
- 高潮；
- 结局；
- 主要人物弧光；
- 主要伏笔。

### 章节规划字段

- 章节编号；
- 章节标题；
- 本章目标；
- 登场人物；
- 场景；
- 时间；
- 核心事件；
- 冲突；
- 信息揭示；
- 人物变化；
- 伏笔操作；
- 开头钩子；
- 结尾钩子；
- 目标字数；
- 状态。

### 功能要求

- 支持树形结构；
- 支持拖动排序；
- 支持局部重新生成；
- 支持批量生成章节；
- 支持折叠和展开；
- 支持从章节反向同步摘要；
- 支持大纲版本；
- 修改大纲时提示受影响章节。

---

## 10.6 章节和正文编辑器

### 编辑器基础能力

- 富文本或 Markdown 编辑；
- 自动保存；
- 字数统计；
- 段落选择；
- 搜索替换；
- 撤销重做；
- 全屏写作；
- 章节切换；
- 注释和批注；
- AI 局部操作；
- 版本历史；
- 导出。

### AI 操作

- 生成正文；
- 续写；
- 重写；
- 润色；
- 扩写；
- 缩写；
- 加强描写；
- 增加对话；
- 调整节奏；
- 改变视角；
- 修复设定冲突；
- 根据批注修改；
- 生成多个版本；
- 分析本章问题。

### 生成前设置

- 模型；
- 目标字数；
- Temperature；
- 叙事视角；
- 文风；
- 章节目标；
- 允许新增设定；
- 是否启用一致性检查；
- 是否生成后自动摘要；
- 启用的 Skill；
- 上下文范围。

---

## 10.7 Skill 管理

### Skill 定义

Skill 是可复用的写作规则和提示词模块。

### Skill 字段

| 字段 | 说明 |
|---|---|
| 名称 | Skill 名称 |
| 描述 | 用途说明 |
| 类型 | 大纲、人物、正文、润色、检查 |
| 提示词 | 实际规则文本 |
| 适用范围 | 全局、项目、人物、章节 |
| 优先级 | 多 Skill 冲突时的顺序 |
| 参数 | 字数、强度、比例等 |
| 默认启用 | 是否默认使用 |
| 版本 | 修改历史 |

### 内置 Skill 示例

- 悬疑节奏；
- 强化对话；
- 场景沉浸；
- 第一人称限制；
- 爽文节奏；
- 减少 AI 腔；
- 细腻情感；
- 快节奏战斗；
- 章节结尾钩子；
- 设定一致性；
- 人物语言差异化。

### 第一版限制

第一版 Skill 仅作为提示词模块，不支持执行任意代码，不设计为第三方插件系统。

---

## 10.8 Prompt 管理

### 提示词层级

1. 系统提示词；
2. 任务提示词；
3. 项目提示词；
4. Skill；
5. 用户本次要求；
6. 输出格式约束；
7. 上下文资料。

### 内置任务模板

- 创意分析；
- 项目提案；
- 世界观生成；
- 人物生成；
- 总纲生成；
- 分卷生成；
- 章节规划；
- 正文生成；
- 正文续写；
- 正文润色；
- 章节摘要；
- 人物状态提取；
- 伏笔提取；
- 一致性检查。

### 功能要求

- 模板变量；
- 预览最终 Prompt；
- 复制最终 Prompt；
- 恢复默认模板；
- 项目级覆盖；
- 模板版本；
- JSON 输出格式校验。

---

## 10.9 上下文管理

### 目标

在不发送整本小说的情况下，为当前任务选择最相关的信息。

### 上下文分类

#### 固定上下文

- 项目简介；
- 核心世界观；
- 写作风格；
- 不可修改规则。

#### 当前任务上下文

- 当前卷大纲；
- 当前章节规划；
- 当前登场人物；
- 当前地点；
- 当前时间点。

#### 近期上下文

- 上一章正文；
- 最近章节摘要；
- 当前剧情阶段摘要。

#### 关联上下文

- 相关人物历史；
- 相关伏笔；
- 相关物品；
- 相关地点；
- 相关设定；
- 相关时间线事件。

#### 用户上下文

- 本次要求；
- 选中的正文；
- 用户批注；
- 手动添加的参考资料。

### 上下文预算

系统应展示：

- 模型上下文上限；
- 当前预计输入 Token；
- 预计输出 Token；
- 各类上下文占比；
- 被裁剪内容；
- 用户手动固定内容。

### 裁剪优先级

建议保留顺序：

1. 当前任务要求；
2. 章节规划；
3. 不可修改规则；
4. 当前人物；
5. 最近正文；
6. 最近摘要；
7. 相关伏笔；
8. 其他背景资料。

---

## 10.10 小说记忆系统

### 短期记忆

- 最近一至两章正文；
- 当前对话；
- 当前编辑内容。

### 中期记忆

- 最近章节摘要；
- 当前分卷摘要；
- 人物当前状态；
- 最近关系变化。

### 长期记忆

- 世界观；
- 人物档案；
- 故事总纲；
- 时间线；
- 地点；
- 势力；
- 物品；
- 伏笔；
- 已确认事实。

### 记忆更新规则

- AI 提取结果默认进入“待确认”状态；
- 用户确认后写入正式记忆；
- 重要事实可锁定；
- 正文被回退时，相关记忆变更也应支持回退；
- 记忆条目需要记录来源章节。

---

## 10.11 版本管理

### 需要版本化的内容

- 世界观；
- 人物；
- 大纲；
- 章节规划；
- 正文；
- Prompt；
- Skill；
- 摘要；
- 记忆变更。

### 功能要求

- 每次 AI 生成自动创建版本；
- 手动保存版本；
- 标记当前生效版本；
- 查看历史版本；
- 恢复旧版本；
- 对比差异；
- 添加版本备注；
- 防止覆盖用户手动修改内容。

### 分支能力

MVP 可暂不实现完整分支，但需要保留以下基础：

- 同一章节保留多个候选版本；
- 用户选择一个版本作为当前版本；
- 其他版本不删除；
- 后续可升级为剧情分支。

---

## 10.12 导入导出

### 导入

第一版支持：

- TXT；
- Markdown；
- 项目备份包。

导入方式：

- 导入为完整正文；
- 按标题或空行拆分章节；
- 导入为参考资料；
- 导入后由 AI 提取人物、大纲和摘要。

### 导出

第一版支持：

- TXT；
- Markdown；
- 完整项目备份包。

第二阶段支持：

- DOCX；
- EPUB；
- PDF；
- 按卷导出；
- 仅导出大纲或人物资料。

---

## 11. AI 系统设计

## 11.1 模型适配器

### 目标

业务功能不直接依赖某个模型供应商。

### 统一接口示例

```text
chat()
generateStructuredData()
generateStreamText()
estimateTokens()
cancelGeneration()
validateConfiguration()
```

### 模型配置字段

- 配置名称；
- 服务商；
- Base URL；
- API Key；
- 模型名称；
- 上下文长度；
- 默认 Temperature；
- 最大输出 Token；
- 是否支持流式输出；
- 是否支持 JSON 模式；
- 超时时间；
- 重试次数。

---

## 11.2 AI 任务类型

| 任务 | 输出类型 | 建议随机性 |
|---|---|---|
| 创意发散 | 文本/列表 | 中高 |
| 世界观生成 | JSON | 中 |
| 人物生成 | JSON | 中 |
| 大纲生成 | JSON | 中低 |
| 章节规划 | JSON | 中低 |
| 正文生成 | 文本 | 中 |
| 润色和改写 | 文本 | 中低 |
| 摘要提取 | JSON | 低 |
| 状态提取 | JSON | 低 |
| 一致性检查 | JSON | 低 |

---

## 11.3 结构化输出

大纲、人物、摘要、时间线和检查结果应优先使用 JSON。

系统需要：

- 校验 JSON；
- 自动去除代码块；
- 支持缺失字段补全；
- 解析失败时自动重试；
- 保留模型原始响应；
- 向用户展示可读错误；
- 不因单次解析失败覆盖正式数据。

---

## 11.4 正文生成 Prompt 结构

```text
系统角色
→ 最高级写作规则
→ 当前任务
→ 项目简介
→ 世界观核心规则
→ 当前章节规划
→ 登场人物
→ 人物当前状态
→ 最近章节摘要
→ 相关历史事件
→ 相关伏笔
→ 已启用 Skill
→ 用户本次要求
→ 输出约束
```

### 正文输出约束

- 仅输出正文；
- 不解释生成过程；
- 不重复章节规划；
- 不擅自改变锁定设定；
- 不提前揭示未授权信息；
- 避免总结式结尾；
- 遵守目标视角；
- 控制目标字数；
- 与上一章自然衔接。

---

## 11.5 生成后处理

正文生成完成后，可执行以下任务：

1. 内容基础校验；
2. 字数统计；
3. 章节摘要；
4. 人物状态变化；
5. 关系变化；
6. 时间线事件；
7. 伏笔变化；
8. 新增设定；
9. 一致性检查；
10. 待确认变更。

为控制成本，用户可选择：

- 快速模式：只生成正文；
- 标准模式：正文 + 摘要；
- 完整模式：正文 + 摘要 + 状态提取 + 一致性检查。

---

## 11.6 一致性检查

### 检查类型

- 人物身份冲突；
- 年龄冲突；
- 地点冲突；
- 时间顺序冲突；
- 已死亡人物出现；
- 人物知道不应知道的信息；
- 物品状态冲突；
- 能力规则冲突；
- 关系变化冲突；
- 大纲偏离；
- 叙事视角错误；
- 未经允许新增核心设定。

### 输出格式

```json
{
  "issues": [
    {
      "type": "character_state_conflict",
      "severity": "high",
      "description": "人物左臂已受伤，但本章出现双手持剑。",
      "source": "第12章",
      "suggestion": "修改动作为单手持剑，或补充伤势已恢复。"
    }
  ]
}
```

### 产品交互

用户可以：

- 忽略；
- 标记为已知问题；
- 让 AI 修复；
- 手动修改；
- 更新正式设定；
- 将问题加入待办。

---

## 12. 数据模型

### 12.1 核心实体

```text
Project
├── ProjectSettings
├── WorldEntry
├── Character
│   ├── CharacterState
│   └── CharacterRelationship
├── Outline
│   ├── Volume
│   └── ChapterPlan
├── Chapter
│   ├── ChapterVersion
│   ├── ChapterSummary
│   └── ChapterMemoryChange
├── TimelineEvent
├── Foreshadowing
├── Location
├── Item
├── Faction
├── Skill
├── PromptTemplate
├── Conversation
├── AIModelConfig
└── AITaskLog
```

### 12.2 关键关系

- 一个项目包含多个世界观条目；
- 一个项目包含多个人物；
- 一个人物可拥有多个状态版本；
- 一个项目包含一个或多个大纲版本；
- 一个大纲包含多个分卷和章节规划；
- 一个章节规划对应一个正式章节；
- 一个章节包含多个正文版本；
- 一个章节可产生多个记忆变更；
- 伏笔、人物、地点和事件可关联多个章节；
- 每次 AI 调用产生一条任务日志。

---

## 13. 页面设计建议

### 13.1 项目首页

- 最近项目；
- 新建项目；
- 导入项目；
- 项目统计；
- 模型连接状态；
- 最近生成任务。

### 13.2 项目概览

- 小说简介；
- 当前进度；
- 总字数；
- 章节数；
- 主要人物；
- 当前故事阶段；
- 未回收伏笔；
- 待处理一致性问题；
- 最近编辑记录。

### 13.3 AI 对话页

- 多轮对话；
- 引用项目资料；
- 对话模板；
- 保存到项目；
- 对话分支；
- 模型选择；
- Skill 选择。

### 13.4 大纲页

- 左侧树形结构；
- 中间节点详情；
- 右侧 AI 建议；
- 批量生成；
- 局部重写；
- 版本对比。

### 13.5 人物页

- 人物卡片列表；
- 人物详情；
- 当前状态；
- 人物弧光；
- 关系；
- 出场记录；
- AI 完善人物。

### 13.6 正文编辑页

- 章节树；
- 正文编辑器；
- AI 工具栏；
- 上下文面板；
- 字数和进度；
- 版本历史；
- 一致性问题；
- 章节摘要。

### 13.7 Skill 页

- 内置 Skill；
- 自定义 Skill；
- Skill 编辑器；
- 适用范围；
- 参数设置；
- 启用状态；
- 版本历史。

### 13.8 设置页

- 模型配置；
- 默认参数；
- 数据目录；
- 自动保存；
- 备份；
- 隐私；
- Prompt 高级设置；
- 日志和故障排查。

---

## 14. 非功能要求

## 14.1 数据安全

- API Key 不以明文形式写入普通配置文件；
- 使用 Windows 安全存储能力保存敏感信息；
- 日志隐藏完整 API Key；
- 项目文件支持手动备份；
- 删除项目时明确提示；
- 应用崩溃后尽可能恢复未保存内容；
- 不默认上传项目数据。

## 14.2 性能

- 万字级章节打开和编辑保持流畅；
- 项目包含数百章节时，章节树仍可正常使用；
- 大列表使用分页或虚拟滚动；
- AI 流式输出不阻塞界面；
- 自动保存不明显影响输入；
- 大型项目加载时展示进度。

## 14.3 稳定性

- AI 请求支持取消；
- 网络失败支持重试；
- 结构化输出解析失败不得破坏项目；
- 所有 AI 写入操作先创建版本；
- 项目写入使用安全事务或临时文件替换；
- 应用异常关闭后可恢复最近状态。

## 14.4 可观测性

AI 任务日志记录：

- 任务类型；
- 使用模型；
- Prompt 模板版本；
- 输入和输出 Token；
- 开始和结束时间；
- 是否成功；
- 错误信息；
- 重试次数；
- 生成内容版本。

---

## 15. MVP 验收标准

### 15.1 项目闭环

用户能够：

1. 新建小说项目；
2. 配置并测试模型；
3. 通过 AI 对话形成创意；
4. 生成并保存人物；
5. 生成并保存大纲；
6. 生成章节规划；
7. 根据项目资料生成正文；
8. 编辑并确认正文；
9. 自动生成章节摘要；
10. 继续生成下一章；
11. 查看并恢复旧版本；
12. 导出小说正文。

### 15.2 AI 稳定性

- 流式输出可正常展示；
- 用户可停止生成；
- 网络错误有明确提示；
- JSON 解析失败可重试；
- 正文生成不会直接覆盖已有内容；
- 每次生成均保留版本；
- 上下文内容可预览；
- 模型未配置时不能误触发调用。

### 15.3 数据安全

- 重启应用后项目完整存在；
- 自动保存有效；
- API Key 不直接显示完整内容；
- 项目支持备份和恢复；
- 删除操作需要二次确认。

---

## 16. 产品版本规划

## 16.1 第一阶段：基础项目和编辑器

目标：建立可运行、可保存的桌面应用。

功能：

- 项目管理；
- 本地数据存储；
- 页面框架；
- 章节树；
- 正文编辑器；
- 自动保存；
- 基础设置；
- TXT 导入导出。

## 16.2 第二阶段：AI 基础能力

目标：完成稳定的模型调用。

功能：

- 模型配置；
- API 连接测试；
- 流式输出；
- 停止生成；
- AI 任务日志；
- 错误处理；
- 模型适配器；
- Token 基础统计。

## 16.3 第三阶段：结构化创作

目标：完成从创意到正文的完整流程。

功能：

- AI 对话；
- 世界观；
- 人物；
- 大纲；
- 章节规划；
- 正文生成；
- 结构化输出解析；
- Prompt 模板。

## 16.4 第四阶段：上下文和记忆

目标：支持长篇连续创作。

功能：

- 章节摘要；
- 人物状态；
- 上下文组装；
- 上下文预算；
- 关联资料选择；
- 生成后记忆更新；
- 基础一致性检查。

## 16.5 第五阶段：专业创作能力

目标：提升可控性和作者效率。

功能：

- 自定义 Skill；
- Prompt 管理；
- 版本对比；
- 人物关系；
- 时间线；
- 伏笔管理；
- 批量章节规划；
- 多模型对比。

## 16.6 第六阶段：智能检索和高级能力

目标：支持超长篇和复杂资料库。

功能：

- Embedding；
- RAG；
- 语义检索；
- 自动检索相关历史内容；
- 内容质量评分；
- 多阶段生成；
- 可配置工作流；
- 有限制的 Agent 能力。

---

## 17. 风险与解决方案

| 风险 | 表现 | 解决方案 |
|---|---|---|
| AI 遗忘设定 | 人物、地点和事件前后不一致 | 结构化数据、摘要、上下文组装、一致性检查 |
| 上下文过长 | 超限、成本高、输出质量下降 | Token 预算、优先级裁剪、关联检索 |
| JSON 不稳定 | 大纲和人物无法解析 | JSON 模式、Schema 校验、自动重试 |
| AI 擅自修改设定 | 人物身份或世界规则变化 | 锁定设定、重要变更待确认 |
| 内容被覆盖 | 新生成内容替换旧稿 | 自动版本、候选内容、回滚 |
| 成本不可控 | 一章触发多次模型调用 | 快速/标准/完整模式、用量统计 |
| 模型接口差异 | 不同服务商格式不一致 | 模型适配器和统一接口 |
| API Key 泄露 | 日志或配置暴露密钥 | 安全存储、日志脱敏 |
| 渲染进程权限限制 | 密钥安全存储、本地文件访问和跨域请求不能由页面直接处理 | 通过 Electron 主进程实现本地能力，并由预加载层暴露最小化类型接口 |
| 功能过度复杂 | 第一版无法交付 | 严格控制 MVP，后置 RAG 和 Agent |

---

## 18. 成功指标

### 18.1 核心产品指标

- 新建项目后成功生成第一章的比例；
- 用户完成三章以上创作的比例；
- AI 生成内容被接受或修改后采用的比例；
- 用户平均每周创作字数；
- 项目连续使用天数；
- 章节生成失败率；
- 设定冲突发现数量和修复率；
- 版本恢复功能使用率。

### 18.2 AI 质量指标

- 结构化输出解析成功率；
- 正文生成完成率；
- 用户重新生成率；
- 用户大幅删除生成内容的比例；
- 人物设定冲突率；
- 时间线冲突率；
- 章节偏离大纲比例；
- 平均单章 Token 和成本。

---

## 19. 关键产品决策

### 19.1 已确认决策

1. Windows 桌面端采用 Electron；
2. 渲染进程采用 Vue 3 + TypeScript；
3. 构建工具采用 electron-vite；
4. 默认遵循 Electron 安全隔离，不在渲染进程中启用 Node.js 集成。

### 19.2 待确认决策

建议在开发前确认以下决策：

1. 第一版是否仅支持 OpenAI 兼容接口；
2. 项目数据使用单文件、SQLite 还是混合存储；
3. 正文编辑器使用 Markdown 还是富文本；
4. 是否允许用户直接编辑内置 Prompt；
5. AI 提取的记忆变更是否必须逐条确认；
6. 第一版是否包含人物关系和伏笔管理；
7. 是否提供快速、标准、完整三种生成模式；
8. 是否允许项目配置覆盖全局模型配置；
9. 项目备份包的格式；
10. 正文生成时默认保留多少近期内容。

---

## 20. MVP 推荐取舍

为了保证第一版能够稳定交付，建议 MVP 聚焦以下主链路：

```text
新建项目
→ 配置模型
→ 生成项目设定
→ 生成人物
→ 生成大纲
→ 生成章节规划
→ 生成正文
→ 保存版本
→ 生成摘要
→ 继续下一章
```

第一版暂时不需要：

- 完整 RAG；
- 向量数据库；
- 多智能体；
- 自动写完整本小说；
- 复杂节点式工作流；
- 云端同步；
- 多人协作；
- 第三方 Skill 插件市场。

产品能否成功的关键，不是第一版功能数量，而是以下四点：

1. AI 调用是否稳定；
2. 项目资料是否结构清晰；
3. 正文生成是否能够正确使用前文和设定；
4. 用户是否能够放心修改、回退和继续创作。

---

## 21. 总结

本产品的核心不是简单封装一个 AI 聊天窗口，而是建立一个围绕小说项目运行的创作系统。

产品的核心能力可以概括为：

> 将用户创意转化为结构化小说资料，根据当前创作任务自动组装相关上下文，通过可配置的提示词和 Skill 调用模型生成内容，再将用户确认后的结果持续更新到小说项目中。

第一阶段应优先建立稳定、可控、可回退的创作闭环。后续再逐步增加 RAG、语义检索、时间线、伏笔管理、多模型协作和高级工作流。
