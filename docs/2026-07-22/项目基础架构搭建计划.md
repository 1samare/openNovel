# 小说 AI 辅助写作项目基础架构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建一个能够在 Windows 本地运行的 Electron + Vue 3 + TypeScript 桌面应用骨架。

**Architecture:** Electron 主进程只负责安全窗口和应用生命周期，预加载层保持空白安全边界，Vue 渲染进程通过 Hash Router 提供项目中心和创作工作台占位页面。共享目录只包含纯 TypeScript 应用常量，不包含业务实体、持久化或 IPC。

**Tech Stack:** Electron 43、Vue 3、TypeScript 6、electron-vite 5、Vite 7、Vue Router 5、Node.js 内置测试运行器、npm

## Global Constraints

- 目标平台为 Windows 10 及以上版本。
- Node.js 必须为 22.12 或更高版本；当前环境为 22.18.0。
- 本阶段不实现业务逻辑、数据库、AI 调用、状态管理、富文本编辑器、应用内配置或安装包构建。
- 渲染进程必须启用 `contextIsolation` 和 `sandbox`，并禁用 `nodeIntegration`。
- 不向渲染进程暴露 Node.js、Electron 或通用 IPC API。
- 新增依赖仅限 Electron、Vue、TypeScript、electron-vite、Vite、Vue Router 和类型检查所需包。
- 修改计划必须保存到 `docs/YYYY-MM-DD/文件名称计划.md`，实施范围变化时先更新本文件。
- 当前目录不是 Git 仓库，因此本计划不包含 commit 步骤。

---

## 文件结构

```text
openNovel/
├── .gitignore                         # 忽略依赖、构建产物和本地日志
├── src/
│   ├── main/index.ts                 # Electron 应用与窗口生命周期
│   ├── preload/index.ts              # 空白安全桥接入口
│   ├── shared/app.ts                 # 跨进程纯应用常量
│   └── renderer/
│       ├── index.html                # 渲染入口与 CSP
│       └── src/
│           ├── assets/base.css       # 全局视觉基础
│           ├── layouts/WorkspaceLayout.vue
│           ├── navigation/items.ts
│           ├── router/index.ts
│           ├── views/ProjectCenterView.vue
│           ├── views/WorkspacePlaceholderView.vue
│           ├── App.vue
│           ├── env.d.ts
│           └── main.ts
├── tests/
│   ├── electron-foundation.test.mjs
│   ├── renderer-shell.test.mjs
│   └── documentation.test.mjs
├── electron.vite.config.ts
├── package.json
├── package-lock.json                  # npm 自动生成的依赖锁文件
├── tsconfig.json
├── tsconfig.node.json
├── tsconfig.web.json
└── README.md
```

### Task 1: Electron 构建与安全进程骨架

**Files:**
- Create: `tests/electron-foundation.test.mjs`
- Create: `.gitignore`
- Create: `package.json`
- Create: `package-lock.json`（由 `npm install` 自动生成）
- Create: `electron.vite.config.ts`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `tsconfig.web.json`
- Create: `src/shared/app.ts`
- Create: `src/main/index.ts`
- Create: `src/preload/index.ts`

**Interfaces:**
- Consumes: Node.js 22.12+、Electron `app` 和 `BrowserWindow`。
- Produces: `APP_NAME: string`、安全 BrowserWindow、空白预加载入口、`dev`/`build`/`start`/`typecheck`/`test` npm 命令。

- [x] **Step 1: 写入 Electron 安全边界测试**

```js
// tests/electron-foundation.test.mjs
import assert from 'node:assert/strict'
import { readFile } from 'node:fs/promises'
import test from 'node:test'

const projectFile = (path) => new URL(`../${path}`, import.meta.url)
const readProjectFile = (path) => readFile(projectFile(path), 'utf8')

test('Electron 窗口采用隔离且无 Node 注入的安全默认值', async () => {
  const source = await readProjectFile('src/main/index.ts')

  assert.match(source, /contextIsolation:\s*true/)
  assert.match(source, /nodeIntegration:\s*false/)
  assert.match(source, /sandbox:\s*true/)
  assert.match(source, /setWindowOpenHandler/)
})

test('预加载层不暴露 Electron 或 Node API', async () => {
  const source = await readProjectFile('src/preload/index.ts')

  assert.doesNotMatch(source, /contextBridge|ipcRenderer|require\s*\(/)
  assert.match(source, /process\.contextIsolated/)
})

test('依赖安装后显式准备 Electron 二进制', async () => {
  const manifest = JSON.parse(await readProjectFile('package.json'))

  assert.equal(manifest.scripts.postinstall, 'install-electron')
  assert.equal(manifest.config.electron_mirror, 'https://npmmirror.com/mirrors/electron/')
})

test('类型检查配置不在项目根目录生成构建缓存', async () => {
  const nodeConfig = JSON.parse(await readProjectFile('tsconfig.node.json'))
  const webConfig = JSON.parse(await readProjectFile('tsconfig.web.json'))

  assert.equal(nodeConfig.compilerOptions.composite, undefined)
  assert.equal(webConfig.compilerOptions.composite, undefined)
})
```

- [x] **Step 2: 运行测试并确认因主进程文件不存在而失败**

Run: `node --test tests/electron-foundation.test.mjs`

Expected: FAIL，错误包含 `ENOENT` 和 `src/main/index.ts`。

- [x] **Step 3: 写入包清单和 TypeScript/electron-vite 配置**

```gitignore
# .gitignore
node_modules/
out/
dist/
*.log
.DS_Store
Thumbs.db
```

```json
// package.json
{
  "name": "open-novel",
  "version": "0.1.0",
  "private": true,
  "description": "本地优先的小说 AI 辅助写作桌面应用",
  "config": {
    "electron_mirror": "https://npmmirror.com/mirrors/electron/"
  },
  "main": "./out/main/index.js",
  "scripts": {
    "postinstall": "install-electron",
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "build": "npm run typecheck && electron-vite build",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck:web": "vue-tsc --noEmit -p tsconfig.web.json",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@vitejs/plugin-vue": "^6.0.8",
    "electron": "^43.1.1",
    "electron-vite": "^5.0.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.6",
    "vue-tsc": "^3.3.7"
  }
}
```

```ts
// electron.vite.config.ts
import { resolve } from 'node:path'
import vue from '@vitejs/plugin-vue'
import { defineConfig } from 'electron-vite'

export default defineConfig({
  main: {
    resolve: {
      alias: {
        '@shared': resolve('src/shared')
      }
    }
  },
  preload: {},
  renderer: {
    resolve: {
      alias: {
        '@renderer': resolve('src/renderer/src'),
        '@shared': resolve('src/shared')
      }
    },
    plugins: [vue()]
  }
})
```

```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2022",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true
  }
}
```

```json
// tsconfig.node.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node", "electron-vite/node"],
    "noEmit": true,
    "paths": {
      "@shared/*": ["./src/shared/*"]
    }
  },
  "include": [
    "electron.vite.config.ts",
    "src/main/**/*.ts",
    "src/preload/**/*.ts",
    "src/shared/**/*.ts"
  ]
}
```

```json
// tsconfig.web.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "noEmit": true,
    "paths": {
      "@renderer/*": ["./src/renderer/src/*"],
      "@shared/*": ["./src/shared/*"]
    }
  },
  "include": ["src/renderer/src/**/*.ts", "src/renderer/src/**/*.vue", "src/shared/**/*.ts"]
}
```

- [x] **Step 4: 写入主进程、预加载层和共享常量**

```ts
// src/shared/app.ts
export const APP_NAME = 'OpenNovel'
export const APP_DESCRIPTION = '小说 AI 辅助写作桌面工作台'
```

```ts
// src/main/index.ts
import { join } from 'node:path'
import { app, BrowserWindow } from 'electron'
import { APP_NAME } from '@shared/app'

const createMainWindow = (): BrowserWindow => {
  const mainWindow = new BrowserWindow({
    width: 1360,
    height: 860,
    minWidth: 1024,
    minHeight: 700,
    show: false,
    autoHideMenuBar: true,
    title: APP_NAME,
    backgroundColor: '#f6f7fb',
    webPreferences: {
      preload: join(__dirname, '../preload/index.js'),
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true
    }
  })

  mainWindow.once('ready-to-show', () => mainWindow.show())
  mainWindow.webContents.setWindowOpenHandler(() => ({ action: 'deny' }))
  mainWindow.webContents.on(
    'did-fail-load',
    (_event, errorCode, errorDescription, validatedURL) => {
      console.error('Renderer load failed', { errorCode, errorDescription, validatedURL })
    }
  )

  if (process.env.ELECTRON_RENDERER_URL) {
    void mainWindow.loadURL(process.env.ELECTRON_RENDERER_URL)
  } else {
    void mainWindow.loadFile(join(__dirname, '../renderer/index.html'))
  }

  return mainWindow
}

app.whenReady().then(() => {
  createMainWindow()

  app.on('activate', () => {
    if (BrowserWindow.getAllWindows().length === 0) {
      createMainWindow()
    }
  })
})

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit()
  }
})
```

```ts
// src/preload/index.ts
if (!process.contextIsolated) {
  console.error('Preload isolation is disabled')
}
```

- [x] **Step 5: 安装依赖并验证 Electron 安全边界测试**

Run: `npm.cmd install`

Expected: 成功生成 `node_modules` 和 `package-lock.json`，退出码为 0。

Run: `npm.cmd run typecheck:node`

Expected: PASS，退出码为 0。

Run: `node --test tests/electron-foundation.test.mjs`

Expected: 4 tests passed，0 failed。

### Task 2: Vue 路由与创作工作台页面骨架

**Files:**
- Create: `tests/renderer-shell.test.mjs`
- Create: `src/renderer/index.html`
- Create: `src/renderer/src/env.d.ts`
- Create: `src/renderer/src/main.ts`
- Create: `src/renderer/src/App.vue`
- Create: `src/renderer/src/navigation/items.ts`
- Create: `src/renderer/src/router/index.ts`
- Create: `src/renderer/src/layouts/WorkspaceLayout.vue`
- Create: `src/renderer/src/views/ProjectCenterView.vue`
- Create: `src/renderer/src/views/WorkspacePlaceholderView.vue`
- Create: `src/renderer/src/assets/base.css`

**Interfaces:**
- Consumes: `APP_NAME`、`APP_DESCRIPTION`、Vue Router。
- Produces: `/` 项目中心；`/workspace/{overview,chat,world,characters,outline,chapters,skills,versions,settings}` 页面路由；`navigationItems` 导航定义。

- [x] **Step 1: 写入渲染进程路由测试**

```js
// tests/renderer-shell.test.mjs
import assert from 'node:assert/strict'
import { readFile } from 'node:fs/promises'
import test from 'node:test'

const projectFile = (path) => new URL(`../${path}`, import.meta.url)
const readProjectFile = (path) => readFile(projectFile(path), 'utf8')

test('创作工作台包含产品设计要求的一级页面', async () => {
  const source = await readProjectFile('src/renderer/src/navigation/items.ts')
  const routes = [
    'overview',
    'chat',
    'world',
    'characters',
    'outline',
    'chapters',
    'skills',
    'versions',
    'settings'
  ]

  for (const route of routes) {
    assert.match(source, new RegExp(`path: '${route}'`))
  }
})

test('Electron 渲染入口定义限制性内容安全策略', async () => {
  const source = await readProjectFile('src/renderer/index.html')

  assert.match(source, /Content-Security-Policy/)
  assert.match(source, /default-src 'self'/)
  assert.match(source, /script-src 'self'/)
})
```

- [x] **Step 2: 运行测试并确认因渲染文件不存在而失败**

Run: `node --test tests/renderer-shell.test.mjs`

Expected: FAIL，错误包含 `ENOENT` 和 `navigation/items.ts`。

- [x] **Step 3: 写入 Vue 应用入口、导航定义和路由**

```html
<!-- src/renderer/index.html -->
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OpenNovel</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

```ts
// src/renderer/src/env.d.ts
/// <reference types="vite/client" />
```

```ts
// src/renderer/src/main.ts
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import './assets/base.css'

const app = createApp(App)

app.config.errorHandler = (error, instance, info) => {
  console.error('Unhandled renderer error', { error, instance, info })
}

app.use(router)
app.mount('#app')
```

```vue
<!-- src/renderer/src/App.vue -->
<template>
  <RouterView />
</template>
```

```ts
// src/renderer/src/navigation/items.ts
export interface NavigationItem {
  path: string
  label: string
  marker: string
  description: string
}

export const navigationItems: readonly NavigationItem[] = [
  { path: 'overview', label: '项目概览', marker: '概', description: '查看小说项目的整体创作状态。' },
  { path: 'chat', label: 'AI 对话', marker: '聊', description: '围绕当前小说项目与 AI 进行讨论。' },
  { path: 'world', label: '世界观', marker: '世', description: '维护世界背景、规则、地点与势力。' },
  { path: 'characters', label: '人物', marker: '人', description: '管理人物档案、关系和当前状态。' },
  { path: 'outline', label: '大纲', marker: '纲', description: '组织故事总纲、分卷与章节规划。' },
  { path: 'chapters', label: '章节', marker: '章', description: '进入章节树与正文创作区域。' },
  { path: 'skills', label: 'Skill', marker: '技', description: '管理可复用的写作规则与提示词模块。' },
  { path: 'versions', label: '版本历史', marker: '版', description: '查看内容版本和恢复入口。' },
  { path: 'settings', label: '设置', marker: '设', description: '管理应用基础设置和数据目录。' }
]
```

```ts
// src/renderer/src/router/index.ts
import { createRouter, createWebHashHistory, type RouteRecordRaw } from 'vue-router'
import WorkspaceLayout from '@renderer/layouts/WorkspaceLayout.vue'
import { navigationItems } from '@renderer/navigation/items'
import ProjectCenterView from '@renderer/views/ProjectCenterView.vue'
import WorkspacePlaceholderView from '@renderer/views/WorkspacePlaceholderView.vue'

const workspaceRoutes: RouteRecordRaw[] = navigationItems.map((item) => ({
  path: item.path,
  name: item.path,
  component: WorkspacePlaceholderView,
  props: {
    title: item.label,
    description: item.description
  }
}))

const router = createRouter({
  history: createWebHashHistory(),
  routes: [
    {
      path: '/',
      name: 'project-center',
      component: ProjectCenterView
    },
    {
      path: '/workspace',
      component: WorkspaceLayout,
      children: [
        { path: '', redirect: '/workspace/overview' },
        ...workspaceRoutes
      ]
    },
    { path: '/:pathMatch(.*)*', redirect: '/' }
  ]
})

export default router
```

- [x] **Step 4: 写入项目中心、工作台布局和占位页面**

```vue
<!-- src/renderer/src/views/ProjectCenterView.vue -->
<script setup lang="ts">
import { APP_DESCRIPTION, APP_NAME } from '@shared/app'
</script>

<template>
  <main class="project-center">
    <header class="project-center__header">
      <div class="brand-mark">N</div>
      <div>
        <strong>{{ APP_NAME }}</strong>
        <p>{{ APP_DESCRIPTION }}</p>
      </div>
    </header>

    <section class="project-center__content">
      <div>
        <span class="eyebrow">DESKTOP FOUNDATION</span>
        <h1>把故事，写成一个完整世界</h1>
        <p class="project-center__intro">
          当前版本已建立桌面端进程边界、页面路由和创作工作台布局。
        </p>
        <RouterLink class="primary-link" to="/workspace/overview">进入架构预览</RouterLink>
      </div>

      <article class="empty-project-card">
        <div class="empty-project-card__icon">＋</div>
        <h2>项目中心</h2>
        <p>小说项目管理功能将在后续业务阶段接入。</p>
      </article>
    </section>
  </main>
</template>
```

```vue
<!-- src/renderer/src/layouts/WorkspaceLayout.vue -->
<script setup lang="ts">
import { APP_NAME } from '@shared/app'
import { navigationItems } from '@renderer/navigation/items'
</script>

<template>
  <div class="workspace-shell">
    <aside class="workspace-sidebar">
      <RouterLink class="workspace-brand" to="/">
        <span class="brand-mark brand-mark--small">N</span>
        <span>{{ APP_NAME }}</span>
      </RouterLink>

      <nav class="workspace-nav" aria-label="创作工作台导航">
        <RouterLink
          v-for="item in navigationItems"
          :key="item.path"
          class="workspace-nav__item"
          :to="`/workspace/${item.path}`"
        >
          <span class="workspace-nav__marker">{{ item.marker }}</span>
          <span>{{ item.label }}</span>
        </RouterLink>
      </nav>

      <div class="workspace-sidebar__footer">
        <span>本地优先</span>
        <small>基础架构版本 0.1.0</small>
      </div>
    </aside>

    <section class="workspace-main">
      <header class="workspace-topbar">
        <div>
          <strong>创作工作台</strong>
          <span>未打开项目</span>
        </div>
        <RouterLink class="secondary-link" to="/">返回项目中心</RouterLink>
      </header>

      <RouterView />
    </section>
  </div>
</template>
```

```vue
<!-- src/renderer/src/views/WorkspacePlaceholderView.vue -->
<script setup lang="ts">
defineProps<{
  title: string
  description: string
}>()
</script>

<template>
  <main class="workspace-page">
    <header class="workspace-page__header">
      <div>
        <span class="eyebrow">MODULE FOUNDATION</span>
        <h1>{{ title }}</h1>
        <p>{{ description }}</p>
      </div>
      <span class="status-pill">架构占位</span>
    </header>

    <section class="placeholder-panel">
      <div class="placeholder-panel__symbol">{{ title.slice(0, 1) }}</div>
      <h2>{{ title }}模块已就位</h2>
      <p>当前仅提供页面入口和布局边界，不包含业务数据与操作。</p>
    </section>
  </main>
</template>
```

- [x] **Step 5: 写入基础样式**

```css
/* src/renderer/src/assets/base.css */
:root {
  color: #23233a;
  background: #f4f5fa;
  font-family:
    Inter, "PingFang SC", "Microsoft YaHei", system-ui, -apple-system, BlinkMacSystemFont,
    "Segoe UI", sans-serif;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
}

* {
  box-sizing: border-box;
}

html,
body,
#app {
  min-width: 320px;
  min-height: 100%;
  margin: 0;
}

body {
  min-height: 100vh;
  background:
    radial-gradient(circle at 15% 10%, rgba(113, 93, 240, 0.12), transparent 28rem),
    #f4f5fa;
}

a {
  color: inherit;
  text-decoration: none;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

.brand-mark {
  display: grid;
  width: 44px;
  height: 44px;
  place-items: center;
  border-radius: 14px;
  color: white;
  font-weight: 800;
  background: linear-gradient(145deg, #8b7cf6, #6651dc);
  box-shadow: 0 10px 24px rgba(103, 81, 220, 0.28);
}

.brand-mark--small {
  width: 34px;
  height: 34px;
  border-radius: 11px;
  font-size: 0.9rem;
}

.eyebrow {
  color: #725fe3;
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.14em;
}

.project-center {
  min-height: 100vh;
  padding: 32px clamp(28px, 5vw, 76px);
}

.project-center__header {
  display: flex;
  align-items: center;
  gap: 14px;
}

.project-center__header strong {
  font-size: 1.05rem;
}

.project-center__header p {
  margin: 4px 0 0;
  color: #77778f;
  font-size: 0.82rem;
}

.project-center__content {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(300px, 0.8fr);
  gap: 64px;
  align-items: center;
  min-height: calc(100vh - 140px);
  max-width: 1180px;
  margin: 0 auto;
}

.project-center h1 {
  max-width: 700px;
  margin: 18px 0;
  font-size: clamp(2.6rem, 6vw, 5.4rem);
  line-height: 1.02;
  letter-spacing: -0.055em;
}

.project-center__intro {
  max-width: 570px;
  margin: 0 0 32px;
  color: #6c6c82;
  font-size: 1.05rem;
  line-height: 1.8;
}

.primary-link,
.secondary-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 12px;
  font-weight: 700;
}

.primary-link {
  padding: 14px 22px;
  color: white;
  background: #705be4;
  box-shadow: 0 12px 28px rgba(112, 91, 228, 0.28);
}

.secondary-link {
  padding: 9px 14px;
  border: 1px solid #e4e3ed;
  color: #5e5d73;
  background: white;
  font-size: 0.82rem;
}

.empty-project-card {
  padding: 44px;
  border: 1px solid rgba(225, 223, 237, 0.9);
  border-radius: 28px;
  background: rgba(255, 255, 255, 0.82);
  box-shadow: 0 24px 80px rgba(41, 38, 74, 0.12);
  backdrop-filter: blur(20px);
}

.empty-project-card__icon {
  display: grid;
  width: 72px;
  height: 72px;
  place-items: center;
  border-radius: 22px;
  color: #705be4;
  background: #efedff;
  font-size: 2rem;
}

.empty-project-card h2 {
  margin: 26px 0 8px;
}

.empty-project-card p {
  margin: 0;
  color: #77778f;
  line-height: 1.7;
}

.workspace-shell {
  display: grid;
  grid-template-columns: 238px minmax(0, 1fr);
  min-height: 100vh;
}

.workspace-sidebar {
  display: flex;
  flex-direction: column;
  padding: 22px 16px;
  border-right: 1px solid #e7e7ef;
  background: rgba(255, 255, 255, 0.92);
}

.workspace-brand {
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 0 8px 20px;
  font-weight: 800;
}

.workspace-nav {
  display: grid;
  gap: 5px;
  margin-top: 10px;
}

.workspace-nav__item {
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 10px 12px;
  border-radius: 11px;
  color: #66667b;
  font-size: 0.9rem;
  transition: 160ms ease;
}

.workspace-nav__item:hover {
  color: #42378b;
  background: #f4f2ff;
}

.workspace-nav__item.router-link-active {
  color: #4e3fb4;
  font-weight: 700;
  background: #eeebff;
}

.workspace-nav__marker {
  display: grid;
  width: 28px;
  height: 28px;
  place-items: center;
  border-radius: 9px;
  color: #6e5cda;
  background: white;
  font-size: 0.72rem;
  box-shadow: inset 0 0 0 1px #e8e5fa;
}

.workspace-sidebar__footer {
  display: grid;
  gap: 4px;
  margin-top: auto;
  padding: 16px 12px 4px;
  color: #727187;
  font-size: 0.8rem;
}

.workspace-sidebar__footer small {
  color: #a09fb0;
}

.workspace-main {
  min-width: 0;
}

.workspace-topbar {
  display: flex;
  min-height: 70px;
  align-items: center;
  justify-content: space-between;
  padding: 0 30px;
  border-bottom: 1px solid #e7e7ef;
  background: rgba(255, 255, 255, 0.76);
  backdrop-filter: blur(16px);
}

.workspace-topbar div {
  display: flex;
  align-items: baseline;
  gap: 12px;
}

.workspace-topbar span {
  color: #9291a3;
  font-size: 0.78rem;
}

.workspace-page {
  min-height: calc(100vh - 70px);
  padding: clamp(28px, 5vw, 64px);
}

.workspace-page__header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 28px;
  max-width: 1020px;
}

.workspace-page__header h1 {
  margin: 12px 0 8px;
  font-size: clamp(2rem, 4vw, 3.5rem);
  letter-spacing: -0.045em;
}

.workspace-page__header p {
  margin: 0;
  color: #77778f;
}

.status-pill {
  padding: 7px 11px;
  border: 1px solid #ded9ff;
  border-radius: 999px;
  color: #6a55dd;
  background: #f3f1ff;
  font-size: 0.72rem;
  font-weight: 700;
  white-space: nowrap;
}

.placeholder-panel {
  display: grid;
  min-height: 360px;
  place-items: center;
  align-content: center;
  max-width: 1020px;
  margin-top: 38px;
  padding: 40px;
  border: 1px dashed #d8d5e9;
  border-radius: 22px;
  text-align: center;
  background: rgba(255, 255, 255, 0.7);
}

.placeholder-panel__symbol {
  display: grid;
  width: 64px;
  height: 64px;
  place-items: center;
  border-radius: 20px;
  color: #6b57df;
  background: #efedff;
  font-size: 1.4rem;
  font-weight: 800;
}

.placeholder-panel h2 {
  margin: 22px 0 8px;
}

.placeholder-panel p {
  margin: 0;
  color: #858497;
}

@media (max-width: 820px) {
  .project-center__content {
    grid-template-columns: 1fr;
    gap: 36px;
    padding: 72px 0;
  }

  .workspace-shell {
    grid-template-columns: 84px minmax(0, 1fr);
  }

  .workspace-brand > span:last-child,
  .workspace-nav__item > span:last-child,
  .workspace-sidebar__footer {
    display: none;
  }

  .workspace-brand,
  .workspace-nav__item {
    justify-content: center;
  }
}
```

- [x] **Step 6: 验证路由测试、渲染类型和生产构建**

Run: `node --test tests/renderer-shell.test.mjs`

Expected: 2 tests passed，0 failed。

Run: `npm.cmd run typecheck:web`

Expected: PASS，退出码为 0。

Run: `npm.cmd run build`

Expected: 主进程、预加载层和渲染进程均构建成功，生成 `out/`。

### Task 3: README 与产品设计文档同步

**Files:**
- Create: `tests/documentation.test.mjs`
- Create: `README.md`
- Modify: `小说AI辅助写作产品设计文档_V1.0.md`
- Modify: `docs/2026-07-22/项目基础架构搭建计划.md`

**Interfaces:**
- Consumes: 实际 npm 命令、目录结构和技术决策。
- Produces: 可直接执行的本地运行说明和与代码一致的产品设计技术方向。

- [x] **Step 1: 写入文档一致性测试**

```js
// tests/documentation.test.mjs
import assert from 'node:assert/strict'
import { readFile } from 'node:fs/promises'
import test from 'node:test'

const projectFile = (path) => new URL(`../${path}`, import.meta.url)
const readProjectFile = (path) => readFile(projectFile(path), 'utf8')

test('README 描述安装、运行、检查和构建命令', async () => {
  const readme = await readProjectFile('README.md')

  for (const command of ['npm install', 'npm run dev', 'npm run typecheck', 'npm test', 'npm run build']) {
    assert.match(readme, new RegExp(command.replaceAll(' ', '\\s+')))
  }
})

test('产品设计文档记录已确认的 Electron 技术方向', async () => {
  const design = await readProjectFile('小说AI辅助写作产品设计文档_V1.0.md')

  assert.match(design, /Electron \+ Vue 3 \+ TypeScript/)
  assert.doesNotMatch(design, /建议技术方向：\*\* Flutter Windows/)
})
```

- [x] **Step 2: 运行测试并确认因 README 不存在而失败**

Run: `node --test tests/documentation.test.mjs`

Expected: FAIL，错误包含 `ENOENT` 和 `README.md`。

- [x] **Step 3: 创建 README**

```markdown
# OpenNovel

OpenNovel 是一个面向 Windows 的本地优先小说 AI 辅助写作桌面应用。本仓库当前交付 Electron + Vue 3 + TypeScript 基础架构，不包含业务功能。

## 技术栈

- Electron
- Vue 3
- TypeScript
- electron-vite
- Vue Router
- npm

## 环境要求

- Windows 10 或更高版本
- Node.js 22.12 或更高版本
- npm 10 或更高版本

## 本地运行

```powershell
npm install
npm run dev
```

`npm run dev` 会启动 Vite 开发服务并打开 Electron 桌面窗口。

项目默认使用 npmmirror 下载 Electron Windows 二进制；如需切换来源，可通过 `ELECTRON_MIRROR` 环境变量覆盖。

## 质量检查

```powershell
npm run typecheck
npm test
npm run build
```

生产构建完成后可预览构建结果：

```powershell
npm start
```

## 目录结构

```text
src/
├── main/       Electron 主进程
├── preload/    安全预加载边界
├── renderer/   Vue 渲染进程
└── shared/     跨进程纯类型与常量
tests/          架构与文档结构测试
docs/            按日期归档的设计和修改计划
```

## 当前范围

已经具备：

- Electron 窗口和应用生命周期；
- 隔离的渲染进程和空白预加载边界；
- Vue 应用、Hash Router 和公共工作台布局；
- 项目中心及产品一级模块占位页面；
- TypeScript 类型检查、结构测试和生产构建命令。

尚未实现：

- 小说项目增删改查；
- 本地数据库和文件持久化；
- AI 模型配置、调用和流式输出；
- 正文编辑器、版本管理和导入导出；
- 应用安装包生成。

## 安全边界

渲染进程启用上下文隔离与沙箱，并关闭 Node.js 集成。预加载层当前不向页面暴露任何 Electron、Node.js 或 IPC 能力。

## 修改计划约定

任何代码、配置或文档修改前，先在 `docs/YYYY-MM-DD/` 下创建对应的 `文件名称计划.md`。完整约束见 `AGENTS.md`。
```

- [x] **Step 4: 更新产品设计文档中的技术决策**

Apply these exact changes to `小说AI辅助写作产品设计文档_V1.0.md`:

```diff
-**建议技术方向：** Flutter Windows、本地优先、用户自备模型 API Key
+**技术方向：** Electron + Vue 3 + TypeScript、本地优先、用户自备模型 API Key
```

在“7. 产品总体架构”标题后加入：

```markdown
### 7.1 桌面端技术实现

- Electron 负责 Windows 桌面容器、窗口生命周期和后续本地系统能力；
- Vue 3 + TypeScript 负责渲染进程界面；
- electron-vite 统一主进程、预加载层和渲染进程构建；
- 渲染进程启用上下文隔离与沙箱，关闭 Node.js 集成；
- 本地能力必须通过预加载层提供经过类型约束的最小接口。
```

将原“7. 产品总体架构”下的后续标题顺延为 `7.2 分层架构`，并在“19. 关键产品决策”标题后加入：

```markdown
### 19.1 已确认决策

1. Windows 桌面端采用 Electron；
2. 渲染进程采用 Vue 3 + TypeScript；
3. 构建工具采用 electron-vite；
4. 默认遵循 Electron 安全隔离，不在渲染进程中启用 Node.js 集成。

### 19.2 待确认决策
```

将风险表中的“纯前端限制”解决方案替换为：

```markdown
| 渲染进程权限限制 | 密钥安全存储、本地文件访问和跨域请求不能由页面直接处理 | 通过 Electron 主进程实现本地能力，并由预加载层暴露最小化类型接口 |
```

- [x] **Step 5: 验证文档测试并执行全量检查**

Run: `node --test tests/documentation.test.mjs`

Expected: 2 tests passed，0 failed。

Run: `npm.cmd test`

Expected: 8 tests passed，0 failed。

Run: `npm.cmd run typecheck`

Expected: PASS，退出码为 0。

Run: `npm.cmd run build`

Expected: PASS，退出码为 0，`out/main`、`out/preload` 和 `out/renderer` 存在。

### Task 4: Windows 本地启动验收与计划回填

**Files:**
- Modify: `docs/2026-07-22/项目基础架构搭建计划.md`

**Interfaces:**
- Consumes: `npm run dev` 和构建产物。
- Produces: 有时间边界的启动证据、最终文件结果和验证结果。

- [x] **Step 1: 启动 Electron 开发模式并采集日志**

Run: `npm.cmd run dev`

Expected: electron-vite 输出主进程、预加载层和渲染进程开发服务启动成功，Electron 进程保持运行且无启动错误。

- [x] **Step 2: 结束验收进程**

只终止本次命令启动的 Electron 和 electron-vite 子进程，不影响其他 Node.js 或 Electron 进程。

Expected: 验收进程正常退出，不遗留监听端口。

- [x] **Step 3: 回填实际结果**

在本文件末尾追加：

```markdown
## 实际结果

- 记录实际创建和修改的文件；
- 记录最终采用的依赖版本；
- 记录与原计划相比发生的必要调整。

## 验证结果

- 记录测试数量和结果；
- 记录类型检查结果；
- 记录生产构建结果；
- 记录 Electron 本地启动与退出结果。
```

- [x] **Step 4: 再次运行全量静态验收**

Run: `npm.cmd test`

Expected: 8 tests passed，0 failed。

Run: `npm.cmd run typecheck`

Expected: PASS，退出码为 0。

Run: `npm.cmd run build`

Expected: PASS，退出码为 0。

## 实际结果

### 已创建文件

- 根配置：`.gitignore`、`package.json`、`package-lock.json`、`electron.vite.config.ts`、三个 TypeScript 配置文件；
- Electron：`src/main/index.ts`、`src/preload/index.ts`、`src/shared/app.ts`；
- Vue：渲染入口、路由、导航定义、工作台布局、项目中心、通用占位页和基础样式；
- 测试：Electron 安全边界、渲染路由和文档一致性三个测试文件；
- 文档：`README.md`，并更新产品设计文档中的技术方向、分层实现、安全风险和已确认决策。

### 实际依赖版本

- Electron 43.2.0；
- electron-vite 5.0.0；
- Vue 3.5.40；
- Vue Router 5.2.0；
- TypeScript 6.0.3；
- Vite 7.3.6；
- vue-tsc 3.3.7。

### 必要调整

- electron-vite 5 的 peer 依赖不支持 Vite 8，因此使用其支持范围内的 Vite 7.3.6；
- vue-tsc 3.3.7 无法加载 TypeScript 7 的新包导出结构，因此使用兼容的 TypeScript 6.0.3；
- Electron 42 起不再自带 postinstall 二进制下载，项目增加 `postinstall: install-electron`；
- 当前环境默认无法访问 GitHub Electron 资产地址，项目配置 Electron 官方支持的 npmmirror 镜像，并允许用 `ELECTRON_MIRROR` 覆盖；
- 空预加载入口会产生 Vite 空 chunk 警告，因此改为只校验上下文隔离状态，仍不暴露任何 API。
- TypeScript 类型检查不使用 project build，无需 `composite`，已移除该选项以避免在根目录生成 `.tsbuildinfo` 缓存。

## 验证结果

- TDD RED：三个测试文件均先因目标文件或约束缺失而失败；
- 自动测试：8 项通过，0 项失败；
- TypeScript：主进程和 Vue 渲染进程类型检查通过；
- 生产构建：`out/main`、`out/preload`、`out/renderer` 均生成成功且无警告；
- 本地启动：electron-vite 完成开发构建，Vite 在 `http://localhost:5173` 提供渲染页面，Electron 主进程和带 `--enable-sandbox` 的渲染进程实际运行，stderr 为空；
- 进程清理：验收启动的 cmd、Node.js、esbuild 和 Electron 进程树已按根 PID 结束。
