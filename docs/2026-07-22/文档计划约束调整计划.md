# 文档计划约束调整计划

## 目标

统一项目后续修改计划的存放位置和内容格式，并将规则固化为项目级约束。

## 修改范围

- 新增根目录 `AGENTS.md`；
- 将已确认的基础架构设计文档迁移到 `docs/2026-07-22/`；
- 删除不符合新目录约定的旧规格文件；
- 后续基础架构实施计划保存到 `docs/2026-07-22/`。

## 实施步骤

1. 在 `AGENTS.md` 中定义计划文档目录、命名和内容要求；
2. 将现有架构设计规格迁移到新目录；
3. 检查项目内 Markdown 文档路径是否符合约定；
4. 在后续实施前生成基础架构搭建计划。

## 验证方式

- 确认 `AGENTS.md` 位于项目根目录；
- 确认计划文件位于 `docs/2026-07-22/`；
- 确认旧的 `docs/superpowers/specs/` 中不再保留本项目规格文件；
- 确认约束明确覆盖代码、配置和文档修改。

## 实际结果

- 已新增根目录 `AGENTS.md`；
- 已将基础架构设计迁移到 `docs/2026-07-22/项目基础架构设计.md`；
- 已删除旧目录中的同一份规格文件；
- 已明确后续实施计划必须保存到日期目录并在完成后记录验证结果。

## 验证结果

- 项目级约束文件和两份当前文档均位于预期位置；
- 约束已覆盖代码、配置和文档修改；
- 新目录规则不存在例外占位或待定项。
