# 后续阶段统一分支与确认后推送约束实施计划

> **供 Agent 实施者使用：** 使用 `superpowers:executing-plans` 逐项执行；完成声明前使用 `superpowers:verification-before-completion`。步骤使用复选框跟踪。

**目标：** 将根工作区切换到阶段 0 最新成果，从该基线建立 `feat/v1.0`，并把阶段 1–8 固定在该分支、无需新增 worktree、用户确认阶段完成后推送的规则写入项目治理入口。

**架构：** `D:\Code\codex\openNovel` 作为阶段 1–8 唯一默认开发工作区，长期分支 `feat/v1.0` 作为 V1 阶段成果的连续集成线。历史 worktree 只保留为已完成或待核实记录，不再作为新阶段入口；每阶段仍使用独立计划、测试门禁和用户完成确认，确认后提交并推送同一远端分支。

**技术栈：** Git、Git worktree 元数据、Markdown 项目治理文档、现有 npm 验证命令。

## 全局约束

- `feat/v1.0` 必须从已完成阶段 0 的最新提交 `0736181` 创建，不从落后的 `main@f68b4f0` 创建。
- 阶段 1–8 默认只在 `D:\Code\codex\openNovel` 的 `feat/v1.0` 上开发，不为阶段任务新增 worktree。
- 现有 `harness-agent-loop` 与 `phase-0-test-foundation` worktree 不在本任务中删除、修改或复用。
- 每阶段仍必须读取 `DEVELOPMENT_PROGRESS.md`、创建当天计划、登记状态并执行该阶段退出门禁。
- 只有阶段开发与验证完成且用户明确确认后，才提交并推送 `feat/v1.0`；不得推送失败、未确认或范围不清的阶段结果。
- 首次推送使用 `git push -u origin feat/v1.0` 建立 upstream；后续只做普通快进推送，不 force-push。
- 不修改产品代码、测试逻辑、依赖或阶段 0 历史提交，不暂存用户原有 `.superpowers/`。

---

## 开工记录

- 已读取进度清单日期：2026-08-07。
- 工作区绝对路径：`D:\Code\codex\openNovel`。
- 当前分支：`feat/v1.0`。
- 基线 HEAD：`0736181`，即阶段 0 完成交接提交；`origin/main` 经 fetch 后为 `31ca681`。
- 并行 worktree：根工作区 `feat/v1.0@0736181`；`D:\Code\codex\openNovel\.worktrees\harness-agent-loop` 为 `feat/harness-agent-loop@31ca681`；`D:\Code\codex\openNovel\.worktrees\phase-0-test-foundation` 为 `codex/phase-0-test-foundation@0736181`。
- 已有修改归属：根工作区仅有未跟踪 `.superpowers/`，属于用户既有内容，本任务不读取、不修改、不暂存；两个历史 worktree 的内容保持原状。
- 当前任务状态：`进行中`。
- 最近验证：上一阶段在 `0736181` 上通过 README、typecheck、Node 90/90、UI 1/1、build、Electron smoke 与 acceptance；本任务已执行 `git fetch --prune origin` 并确认远端不存在 `feat/v1.0`。
- 阻塞项：无。
- 可直接执行的下一步：更新 `AGENTS.md`，加入统一阶段分支、单工作区与用户确认后推送规则。

## 修改范围与明确不包含的内容

### 修改范围

- 从 `0736181` 创建并切换根工作区分支 `feat/v1.0`。
- 在 `AGENTS.md` 中新增阶段 1–8 的统一分支、工作区和推送门禁。
- 更新 `DEVELOPMENT_PROGRESS.md` 的使用规则、工作区登记、活动任务、风险和交接记录。
- 更新根 `README.md` 与 `docs/2026-08-07/README.md` 的变更同步记录。
- 验证、提交并首次推送 `feat/v1.0`，建立 `origin/feat/v1.0` upstream。

### 明确不包含

- 不开始阶段 1 的数据库、项目生命周期或界面开发。
- 不合并或改写 `main`，不创建 PR，不删除任何现有分支或 worktree。
- 不清理、暂存或提交 `.superpowers/`。
- 不改变阶段 0 的代码、依赖、测试或验收结果。

## 文件结构与职责

- `AGENTS.md`：项目级强制协作规则，定义阶段 1–8 的唯一默认分支、工作区和确认后推送门禁。
- `DEVELOPMENT_PROGRESS.md`：记录当前长期开发分支、历史 worktree、任务状态、验证与阶段 1 入口。
- `README.md`：同步根目录治理规则变更。
- `docs/2026-08-07/后续阶段统一分支与确认后推送约束计划.md`：保存本次分支治理设计、步骤和实际结果。
- `docs/2026-08-07/README.md`：同步本日期目录文档清单与变更记录。

---

### Task 1：建立最新 V1 长期分支

**文件：** 无产品文件修改；只更新 Git 分支状态。

**接口：**

- 输入：阶段 0 完成交接提交 `0736181`。
- 输出：根工作区分支 `feat/v1.0@0736181`，首次提交前无 upstream。

- [x] **Step 1：刷新远端引用并核对分支名未占用**

运行：

```powershell
git fetch --prune origin
git ls-remote --heads origin feat/v1.0
```

实际：fetch 退出 0；远端查询无匹配输出，`feat/v1.0` 未被占用。

- [x] **Step 2：从最新阶段 0 提交创建并切换分支**

运行：`git switch -c feat/v1.0 0736181`

实际：根工作区已切换为 `feat/v1.0@0736181`；未跟踪 `.superpowers/` 保持原状。

### Task 2：写入统一分支与推送约束

**文件：**

- 修改：`AGENTS.md`
- 修改：`DEVELOPMENT_PROGRESS.md`
- 修改：`README.md`
- 修改：`docs/2026-08-07/README.md`

**接口：**

- 输入：用户批准的长期单分支设计。
- 输出：阶段 1–8 开工者可直接执行且无歧义的分支、工作区、验证、确认和推送规则。

- [x] **Step 1：在 `AGENTS.md` 增加长期分支规则**

规则必须明确：根工作区、`feat/v1.0`、不得为普通阶段新建 worktree、历史 worktree 不自动删除、用户确认完成后才推送、禁止 force-push。

- [x] **Step 2：更新进度清单**

把根工作区登记为 `feat/v1.0@0736181 + 本任务工作树`，登记本治理任务为 `进行中`，保留两个历史 worktree 的独立记录，并把阶段 1 下一入口指向根工作区和 `feat/v1.0`。

- [x] **Step 3：同步 README**

根 README 记录阶段 1–8 统一使用 `feat/v1.0`；日期目录 README 列出本计划并记录规则变更。

### Task 3：验证、提交并首次推送

**文件：**

- 修改：`docs/2026-08-07/后续阶段统一分支与确认后推送约束计划.md`
- 修改：`docs/2026-08-07/README.md`
- 修改：`DEVELOPMENT_PROGRESS.md`
- 修改：`README.md`

**接口：**

- 输入：最终文档差异和用户已给出的推送授权。
- 输出：本地提交、干净工作树、`origin/feat/v1.0` 与本地 upstream。

- [x] **Step 1：执行文档与仓库验证**

```powershell
npm.cmd ci
npm.cmd run check:readmes
npm.cmd test
git diff --check
git status --short
```

通过标准：锁文件依赖可干净重建；README 契约退出 0；Node 90/90 与 UI 1/1 通过；空白检查退出 0；状态中只有本任务文件和用户原有 `.superpowers/`。

- [ ] **Step 2：核对并提交本任务文件**

```powershell
git add AGENTS.md DEVELOPMENT_PROGRESS.md README.md docs/2026-08-07/README.md docs/2026-08-07/后续阶段统一分支与确认后推送约束计划.md
git diff --cached --check
git commit -m "docs: standardize v1 development branch workflow"
```

通过标准：暂存范围不含 `.superpowers/`、产品代码、依赖或历史 worktree 文件。

- [ ] **Step 3：首次推送并验证 upstream**

```powershell
git push -u origin feat/v1.0
git rev-parse --abbrev-ref --symbolic-full-name '@{upstream}'
git status --short --branch
```

通过标准：远端分支创建成功；upstream 为 `origin/feat/v1.0`；本地与远端无 ahead/behind；除 `.superpowers/` 外无未提交变更。

## 验证方式与通过标准

- `git merge-base --is-ancestor 0736181 feat/v1.0`：确认分支包含阶段 0 最新成果。
- `npm.cmd ci`：根工作区按阶段 0 最新锁文件重建依赖成功。
- `npm.cmd run check:readmes`：README 目录与同步契约通过。
- `npm.cmd test`：Node 与 UI 测试全部通过。
- `git diff --check` 与 `git diff --cached --check`：无空白错误。
- `git diff --name-only 0736181..HEAD`：只包含本计划列出的治理文档。
- `git status --short --branch`：本地分支跟踪 `origin/feat/v1.0`，只保留用户既有未跟踪 `.superpowers/`。

## 实际结果与验证结果

- Task 1 已完成：远端刷新后确认最新成果为本地 `0736181`，远端未占用 `feat/v1.0`；根工作区已从该提交创建并切换到 `feat/v1.0`。
- Task 2 已完成：`AGENTS.md`、`DEVELOPMENT_PROGRESS.md` 与两级 README 已明确阶段 1–8 固定根工作区和 `feat/v1.0`、不新增常规阶段 worktree、用户确认后推送、禁止 force-push 和不得推送到 `main`。
- Task 3 依赖与静态验证：`npm.cmd ci` 成功安装 217 个包；`npm.cmd run check:readmes` 和 `git diff --check` 退出 0；`git merge-base --is-ancestor 0736181 feat/v1.0` 退出 0。
- Task 3 测试诊断：沙箱内 `npm.cmd test` 的 Node 90/90 通过，但随后 Vitest fork worker 两次在 60 秒启动超时；相同 `test:ui` 单独连续三次通过。只读进程审计确认无 OpenNovel Node 子进程残留；相同聚合命令在沙箱外以 Node 90/90、UI 1/1 退出 0，判定为沙箱对子进程密集测试的调度影响，不修改阶段 0 测试配置。
- Task 3 提交与推送结果将在创建提交和 upstream 后继续回填。
