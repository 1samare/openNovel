# 阶段 0 基线冻结与测试骨架实施计划

> **供 Agent 实施者使用：** 必须使用 `superpowers:executing-plans` 逐项执行；功能和缺陷修复使用 `superpowers:test-driven-development`，完成声明前使用 `superpowers:verification-before-completion`。步骤使用复选框跟踪。

**目标：** 在不改变现有 Harness 运行行为的前提下，修复生产 Electron smoke 的 Windows 子进程缺陷，并建立 Node 单元测试、Vitest Vue 组件测试和 Electron 验收测试的稳定入口。

**架构：** 现有 `tests/*.test.mjs` 继续由 Node Test Runner 执行；新增 `tests/ui/` 由 Vitest、Vue Test Utils 和 happy-dom 执行，两个测试体系不迁移、不互相发现测试文件。真实 Electron smoke 保留独立入口，并作为阶段 0 的 acceptance 基线。

**技术栈：** Electron 43、Vue 3、TypeScript 6、Vite 7、Node Test Runner、Vitest、Vue Test Utils、happy-dom、现有原生 CDP Electron smoke。

## 全局约束

- 只完成阶段 0，不引入小说领域模型、数据库、真实模型调用、编辑器或安装包。
- 保留现有 Node Test Runner 契约测试，不迁移、不删除、不改写为 Vitest。
- `npm test` 必须串行运行 `test:unit` 和 `test:ui`，不得启动真实 Electron 窗口。
- `test:electron-smoke` 保持真实生产构建与 Electron 闭环；`test:acceptance` 在阶段 0 指向该现有验收入口。
- 新增目录必须先有 README；修改任意维护目录文件时同步更新同目录 README。
- 不修改现有 Harness 业务行为、IPC 契约、Mock 文本、状态机或渲染交互。

---

## 开工记录

- 已读取进度清单日期：2026-08-07。
- 工作区绝对路径：`D:\Code\codex\openNovel`。
- 当前分支：`main`。
- 基线 HEAD：`31ca681`。
- 并行 worktree：主工作区 `main@31ca681`；`D:\Code\codex\openNovel\.worktrees\harness-agent-loop` 为 `feat/harness-agent-loop@31ca681`，所有者仍需保留，阶段 0 不使用或修改该 worktree。
- 已有修改归属：主工作区包含尚未提交的 Electron smoke/CI 变更，以及多 Agent 设计、开发治理与进度清单变更；用户已于 2026-08-07 明确批准修复、验证和提交这些前置变更，再创建阶段 0 独立 worktree。
- 当前阶段状态：`进行中`。
- 最近验证：`npm.cmd run check:readmes` 通过；`npm.cmd test` 89/89 通过；`npm.cmd run typecheck` 通过；`npm.cmd run build` 在沙箱外通过；`npm.cmd run test:electron-smoke` 在 build 阶段以 `spawn EINVAL` 失败。
- 阻塞项：无。`spawn EINVAL` 已通过最小复现定位为 Windows 上直接 `spawn('npm.cmd', ...)` 的子进程启动缺陷，纳入本计划 Task 1。
- 可直接执行的下一步：先在 `tests/electron-smoke.test.mjs` 添加 Windows npm CLI 调用回归测试并观察 RED。

### Worktree 切换记录

- 切换日期：2026-08-07。
- 工作区绝对路径：`D:\Code\codex\openNovel\.worktrees\phase-0-test-foundation`。
- 分支与基线：`codex/phase-0-test-foundation` / `f68b4f0`。
- 前置提交：`f68b4f0 test: establish production smoke and phase roadmap`。
- 切换时状态：`git status --short` 为空；`npm.cmd ci` 成功安装 134 个包。
- 并行 worktree：保留 `main@f68b4f0` 和 `feat/harness-agent-loop@31ca681`，本任务不修改后者。
- 下一步：在该隔离 worktree 运行完整干净基线。

## 修改范围与明确不包含的内容

### 修改范围

- 修复 Electron smoke 在 Windows 上直接启动 `npm.cmd` 导致的 `EINVAL`，并回填既有 smoke 计划。
- 增加 Vitest、Vue Test Utils 和 happy-dom 开发依赖并锁定到 `package-lock.json`。
- 新增 Vitest 配置、UI 测试初始化、测试专用 Vue 单文件组件和最小挂载测试。
- 统一 `test:unit`、`test:ui`、`test:electron-smoke`、`test:acceptance` 和 `npm test` 脚本。
- 复用并验证现有 README 目录契约测试；新增 `tests/ui/README.md`。
- 保存基线和最终验证结果，更新本计划、目录 README 与 `DEVELOPMENT_PROGRESS.md`。

### 明确不包含

- 不改动 `src/` 下任何 Harness、Electron、Preload 或 Vue 产品代码。
- 不增加覆盖率服务、浏览器自动化框架、快照测试、测试 UI 或 watch 常驻进程。
- 不创建阶段 1 的项目目录、SQLite Worker 或业务接口。
- 不删除或复用 `feat/harness-agent-loop` worktree。
- 不推送远端、不创建 PR、不合并到远端分支。

## 文件结构与职责

- `scripts/electron-smoke-process.mjs`：生成可跨 Windows 执行的 npm 脚本调用，并管理受跟踪子进程。
- `tests/electron-smoke.test.mjs`：覆盖 Windows npm CLI 调用、Electron 参数、诊断和事件断言。
- `vitest.config.ts`：只发现 `tests/ui/**/*.spec.ts`，启用 Vue SFC 插件和 happy-dom。
- `tests/ui/setup.ts`：为每个 UI 测试自动卸载 Vue wrapper。
- `tests/ui/ComponentHarness.vue`：只用于证明 Vue SFC 编译和 DOM 挂载的最小测试夹具。
- `tests/ui/component-harness.spec.ts`：验证 Vue Test Utils、SFC 转换和 happy-dom 测试环境可执行。
- `tests/ui/README.md`：说明 UI 测试目录用途、依赖边界和维护规则。
- `package.json` / `package-lock.json`：锁定依赖并定义统一测试入口。
- `tsconfig.node.json`：把 `vitest.config.ts` 纳入 Node 配置类型检查。
- `tsconfig.web.json`：把 `tests/ui/**/*.ts` 和 `tests/ui/**/*.vue` 纳入 Vue/DOM 类型检查。
- `tests/README.md`、`scripts/README.md`、根 `README.md`、日期目录 `README.md`：同步测试入口和维护说明。
- `DEVELOPMENT_PROGRESS.md`：记录阶段状态、验证、变更归属和下一入口。

---

### Task 1：修复 Windows Electron smoke 构建子进程

**文件：**

- 修改：`tests/electron-smoke.test.mjs`
- 修改：`scripts/electron-smoke-process.mjs`
- 修改：`scripts/electron-smoke.mjs`
- 修改：`scripts/README.md`
- 修改：`docs/2026-07-28/Electron生产冒烟自动化计划.md`
- 修改：`docs/2026-07-28/README.md`

**接口：**

- 输入：npm 脚本名、`process.execPath`、`process.env.npm_execpath` 和当前平台。
- 输出：`{ command: string, args: string[] }`，优先使用 Node 执行 npm CLI；Windows 缺少 npm CLI 路径时使用固定 `cmd.exe /d /s /c` 回退，绝不把动态用户内容拼入命令。

- [x] **Step 1：写入失败回归测试**

```js
test('builds a Windows npm invocation without spawning npm.cmd directly', () => {
  assert.deepEqual(buildNpmScriptInvocation('build', {
    platform: 'win32',
    nodeExecutable: 'C:\\node.exe',
    npmExecutable: 'C:\\npm-cli.js'
  }), {
    command: 'C:\\node.exe',
    args: ['C:\\npm-cli.js', 'run', 'build']
  })
})
```

- [x] **Step 2：运行测试并确认 RED**

运行：`node --experimental-strip-types --test tests/electron-smoke.test.mjs`

预期：因 `buildNpmScriptInvocation` 尚未导出而失败；失败来自缺失行为而非语法错误。

- [x] **Step 3：实现最小安全调用构造并接入 build**

```js
export const buildNpmScriptInvocation = (script, {
  platform = process.platform,
  nodeExecutable = process.execPath,
  npmExecutable = process.env.npm_execpath,
  commandInterpreter = process.env.ComSpec ?? 'cmd.exe'
} = {}) => {
  if (npmExecutable) {
    return { command: nodeExecutable, args: [npmExecutable, 'run', script] }
  }
  if (platform === 'win32') {
    return { command: commandInterpreter, args: ['/d', '/s', '/c', `npm.cmd run ${script}`] }
  }
  return { command: 'npm', args: ['run', script] }
}
```

`runBuild` 只消费该返回值并交给 `runCommand`；脚本名固定为内部常量 `build`。

- [x] **Step 4：验证 GREEN 与真实 smoke**

运行：

```powershell
node --experimental-strip-types --test tests/electron-smoke.test.mjs
npm.cmd run test:electron-smoke
```

预期：聚焦测试全部通过；真实 smoke 输出 `Electron production smoke passed`。

- [x] **Step 5：回填 smoke 计划并提交前置变更**

分别核对并提交开发治理/设计文档与 Electron smoke/CI 变更，提交前运行其计划列出的五项质量命令；不得暂存 `.superpowers/`。

---

### Task 2：建立独立阶段 0 worktree 和干净基线

**文件：** 无产品文件修改；只创建 Git worktree。

**接口：**

- 输入：已验证且提交后的 `main` HEAD。
- 输出：`D:\Code\codex\openNovel\.worktrees\phase-0-test-foundation`，分支 `codex/phase-0-test-foundation`。

- [x] **Step 1：验证 `.worktrees/` 已忽略**

运行：`git check-ignore -q .worktrees`

预期：退出码 0。

- [x] **Step 2：创建阶段 worktree**

运行：`git worktree add .worktrees/phase-0-test-foundation -b codex/phase-0-test-foundation`

预期：新 worktree 基于已验证前置提交，`git status --short` 为空。

- [x] **Step 3：安装锁文件依赖并验证基线**

运行：

```powershell
npm.cmd ci
npm.cmd test
npm.cmd run typecheck
npm.cmd run build
npm.cmd run test:electron-smoke
npm.cmd run check:readmes
```

预期：五项阶段基线全部通过；如构建目录受沙箱权限限制，只允许在已获准的沙箱外执行相同命令并记录差异。

---

### Task 3：建立 Vue UI 组件测试入口

**文件：**

- 创建：`vitest.config.ts`
- 创建：`tests/ui/README.md`
- 创建：`tests/ui/setup.ts`
- 创建：`tests/ui/ComponentHarness.vue`
- 创建：`tests/ui/component-harness.spec.ts`
- 修改：`package.json`
- 修改：`package-lock.json`
- 修改：`tsconfig.node.json`
- 修改：`tsconfig.web.json`
- 修改：`tests/README.md`
- 修改：`README.md`

**接口：**

- `npm run test:unit`：执行现有 `tests/*.test.mjs`。
- `npm run test:ui`：执行 `tests/ui/**/*.spec.ts`。
- `npm test`：依次执行 `test:unit` 与 `test:ui`。
- `npm run test:acceptance`：阶段 0 复用 `test:electron-smoke`。

- [x] **Step 1：创建 UI 测试与 README 后确认 RED**

测试使用 Vue Test Utils 挂载 `ComponentHarness.vue`，断言可观察 DOM 文本和 `window.happyDOM` 存在。

运行：`npm.cmd run test:ui`

预期：因 `test:ui` 脚本和 Vitest 依赖尚不存在而失败。

- [x] **Step 2：安装最小测试依赖**

运行：`npm.cmd install --save-dev vitest @vue/test-utils happy-dom`

预期：只新增三项直接开发依赖及其传递依赖，`package-lock.json` 同步更新。

- [x] **Step 3：新增 Vitest 配置与初始化**

```ts
import vue from '@vitejs/plugin-vue'
import { defineConfig } from 'vitest/config'

export default defineConfig({
  plugins: [vue()],
  test: {
    environment: 'happy-dom',
    include: ['tests/ui/**/*.spec.ts'],
    setupFiles: ['./tests/ui/setup.ts'],
    clearMocks: true,
    restoreMocks: true
  }
})
```

`setup.ts` 使用 Vue Test Utils 的 `enableAutoUnmount(afterEach)`，不把测试专用清理逻辑放入产品组件。

同时把 `vitest.config.ts` 加入 `tsconfig.node.json`，把 `tests/ui/**/*.ts` 与 `tests/ui/**/*.vue` 加入 `tsconfig.web.json`，保证 `npm run typecheck` 覆盖新测试配置和测试源码。

- [x] **Step 4：统一 npm scripts**

```json
{
  "test": "npm run test:unit && npm run test:ui",
  "test:unit": "node --experimental-strip-types --test tests/*.test.mjs",
  "test:ui": "vitest run",
  "test:electron-smoke": "node scripts/electron-smoke.mjs",
  "test:acceptance": "npm run test:electron-smoke"
}
```

- [x] **Step 5：验证 GREEN**

运行：

```powershell
npm.cmd run test:ui
npm.cmd test
```

预期：UI 测试至少 1 项通过；原有 Node 测试与 UI 测试按顺序全部通过，常规测试不启动 Electron。

---

### Task 4：验证 README 契约与阶段退出标准

**文件：**

- 修改：`docs/2026-08-07/阶段0基线冻结与测试骨架计划.md`
- 修改：`docs/2026-08-07/README.md`
- 修改：`DEVELOPMENT_PROGRESS.md`
- 修改：`README.md`

**接口：**

- 输入：全部实现和实际命令输出。
- 输出：计划实际结果、阶段状态、最近验证、未提交变更归属和阶段 1 的可执行入口。

- [x] **Step 1：运行完整阶段验收**

```powershell
npm.cmd run check:readmes
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run test:electron-smoke
npm.cmd run test:acceptance
git diff --check
```

通过标准：所有命令退出码 0；Node 与 UI 测试均报告明确通过数量；真实 smoke 完成三段 Run 场景；构建产物仍为 main、preload 和 renderer 三部分。

- [x] **Step 2：完成要求逐项审计**

- 基线结果区分既有 `spawn EINVAL` 与阶段 0 结果。
- Node Test Runner 测试仍存在且由 `test:unit` 执行。
- Vitest、Vue Test Utils、happy-dom 已锁定且最小 SFC 测试可执行。
- 五个测试脚本名称存在，`npm test` 串行 unit/UI。
- 新目录 README 契约通过。
- Harness 产品代码和构建入口未改变。

- [x] **Step 3：回填计划和进度清单**

把阶段 0 标记为 `已完成`，记录最终 worktree、分支、短 HEAD、验证命令、结果、未提交变更归属、阻塞项和下一步“创建阶段 1 当天计划并重新核对 Git 状态”。

- [x] **Step 4：提交阶段 0**

```powershell
git add README.md package.json package-lock.json tsconfig.node.json tsconfig.web.json vitest.config.ts tests
git commit -m "test: establish phase zero test foundation"

# 获得实现提交号后，再回填并独立提交计划与进度交接文档。
git add DEVELOPMENT_PROGRESS.md README.md docs/2026-08-07
git commit -m "docs: complete phase zero progress handoff"
```

提交前再次确认暂存范围不包含 `.superpowers/` 或其他 worktree 文件。

## 验证方式与通过标准

- `npm.cmd run check:readmes`：通过 README 目录与同步契约。
- `npm.cmd run typecheck`：Node 与 Vue TypeScript 检查通过。
- `npm.cmd test`：先执行全部 Node 契约测试，再执行 UI 组件测试，退出码 0。
- `npm.cmd run build`：Electron main、preload、renderer 生产构建通过。
- `npm.cmd run test:electron-smoke`：真实生产 Electron smoke 通过。
- `npm.cmd run test:acceptance`：阶段 0 验收入口通过。
- `git diff --check`：无空白错误。
- `git status --short`：仅保留已明确归属的未提交文件；完成提交后阶段 worktree 为空。

## 实际结果与验证结果

- Task 1 RED：聚焦 smoke 逻辑测试共 7 项，6 项通过；Windows npm 调用测试因行为尚未实现而得到 `undefined`，按预期失败。
- Task 1 GREEN：最小实现后聚焦测试 7/7，Node 全量测试 90/90，README 契约和 typecheck 均通过。
- 生产复验：沙箱外 `npm.cmd run build` 通过；`npm.cmd run test:electron-smoke` 输出 `Electron production smoke passed`。既有 `spawn EINVAL` 已修复，不再作为阶段 0 阻塞。
- Task 1 前置提交：设计、进度治理、Electron smoke/CI 和阶段 0 计划以 `f68b4f0` 提交；`.superpowers/` 未暂存。
- Task 2 worktree：`.worktrees/` 忽略检查通过；已创建 `codex/phase-0-test-foundation@f68b4f0`，`npm.cmd ci` 安装 134 个包成功。
- Task 2 干净基线：`npm.cmd test` 90/90、`npm.cmd run typecheck`、`npm.cmd run check:readmes`、`npm.cmd run build` 和 `npm.cmd run test:electron-smoke` 全部通过；新 worktree 的构建可在沙箱内直接写入 `out/`。
- Task 3 RED：创建 `tests/ui/README.md`、最小 Vue SFC 和测试后，`npm.cmd run test:ui` 因脚本尚不存在退出 1，符合配置行为缺失的预期。
- Task 3 依赖准入：Vitest 4.1.10（Node `^20 || ^22 || >=24`、Vite `^6 || ^7 || ^8`）、Vue Test Utils 2.4.11（Vue 3 peer）、happy-dom 20.11.1（Node >=20）均为 MIT；注册表解包体积约 1.9 MB、1.5 MB、8.6 MB，2026-06/07 仍有发布更新。锁文件固定三项解析版本，首次安装新增 83 个包。
- Task 3 GREEN：`npm.cmd run test:ui` 通过 1 个文件、1 项测试；冷启动首次超过命令观察窗口，复跑 1.97 秒完成，未发现残留 Vitest 进程。`npm.cmd test` 明确串行完成 Node 90/90 与 UI 1/1；`npm.cmd run typecheck` 覆盖 Vitest 配置和 UI 测试源码；README 契约通过。
- Task 3 构建与验收：引入依赖后 `npm.cmd run build` 仍产出 main、CommonJS preload、renderer；`npm.cmd run test:electron-smoke` 与 `npm.cmd run test:acceptance` 均输出 `Electron production smoke passed`。
- Task 4 审计：`src/` 无差异；五个测试脚本值与计划一致；三项直接依赖由 `npm ls --depth=0` 确认；Vitest 只发现 `tests/ui/**/*.spec.ts`；README 契约通过。
- Task 4 最终复现：使用更新后的 `package-lock.json` 执行 `npm.cmd ci` 成功安装 217 个包；随后 `npm.cmd run check:readmes`、`npm.cmd run typecheck`、`npm.cmd test`（Node 90/90 + UI 1/1）、`npm.cmd run build`、`npm.cmd run test:electron-smoke`、`npm.cmd run test:acceptance` 和 `git diff --check` 全部退出 0。构建继续产出 main、CommonJS preload 和 renderer，两个真实 Electron 入口均输出 `Electron production smoke passed`。
- Task 4 提交：测试骨架实现已提交为 `80e2581 test: establish phase zero test foundation`；完成进度、计划结果和 README 作为独立交接文档提交，不混入产品或测试实现。
- 阶段结论：阶段 0 已完成，无阻塞项；阶段 1 的直接入口是创建执行当天的修改计划，并按 `DEVELOPMENT_PROGRESS.md` 重新核对 Git 与全部 worktree 状态。
